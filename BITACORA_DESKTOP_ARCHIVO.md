# Bitácora Colaborativa: Gemini ↔ Claude

Este documento es el registro obligatorio de toda actividad técnica realizada en el proyecto. Su propósito es garantizar la sincronización total y la auditoría exhaustiva entre los dos modelos de IA bajo la supervisión del **Project Manager**.

## Protocolo de Trabajo Obligatorio

1.  **Registro de Entrada:** Antes de comenzar cualquier tarea, cada IA debe leer la última entrada de esta bitácora.
2.  **Auditoría Cruzada:** Si hay una tarea pendiente de revisión por la otra IA, **no se puede avanzar** en código nuevo sin antes auditar meticulosamente lo anterior y dejar un comentario de "Aprobado" o "Necesita Ajustes" en esta bitácora.
3.  **Registro de Salida:** Al finalizar un turno, la IA debe registrar:
    *   **Fecha/Hora:**
    *   **Autor:** (Gemini o Claude)
    *   **Archivos Modificados:**
    *   **Resumen Técnico:** Qué se hizo y por qué.
    *   **Estado para la siguiente IA:** (Ej: "Listo para revisión", "Bloqueado por X", "Continuar con Fase Y").

---

## Historial de Cambios y Revisiones

| Fecha/Hora | Autor | Acción / Resumen | Archivos | Estado / Auditoría |
| :--- | :--- | :--- | :--- | :--- |
| 2026-04-11 | Gemini | Creación de Estructura Inicial, Roadmap y Protocolo de Colaboración. | `Estructura_General...`, `Proyecto_Final...`, `Instrucciones_Claude...`, `Bitácora_Colaborativa.md` | **Revisado y Aprobado por Claude** |
| 2026-04-11 | Claude | **Auditoría Inicial Completa.** Se identificaron 7 riesgos críticos (UUID, Decimal, Multi-tenant, Sync). Propuesta de Stack: Next.js + FastAPI + Supabase. | `BITACORA_COLABORATIVA.md` | **Revisado y Aprobado por Gemini** |
| 2026-04-11 | Gemini | **Actualización a Plan Maestro v4.** Integración de **Gemma 4** como motor local para OCR y Chatbot offline. Consolidación de stack híbrido (Local ↔ Nube). | `Proyecto_Final_Arquitectura_Universal.md` | **Revisado y Aprobado por Claude** |
| 2026-04-11 | Claude | **FASE 1 COMPLETADA — Cimientos SQL + Backend FastAPI.** Archivos entregados: (1) `db/esquema_bd.sql` v4: 12 tablas + 2 vistas + índices + seed. Tablas: empresas, roles_usuario, usuarios, clientes, proveedores, plan_cuentas, categorias_inventario, inventario, tipos_comprobante, comprobantes, detalle_comprobantes, pagos, cuentas_banco, movimientos_banco, auditoria_log, sync_queue. Vistas: v_saldo_clientes, v_saldo_proveedores. UUID en todas las PKs. DECIMAL(15,2) en todos los montos. RLS-ready por empresa_id. Campo timbrado_id nullable (expansión futura). IVA paraguayo 0/5/10% en detalle. (2) `backend/main.py`: FastAPI + CORS + Swagger en /docs. (3) `backend/core/`: config.py (Ollama+Gemini+JWT), database.py (async SQLAlchemy), security.py (bcrypt+JWT+bloqueo viewer). (4) `backend/models/schemas.py`: modelos Pydantic con Decimal estricto. (5) `backend/routers/`: auth.py, clientes.py, comprobantes.py (con Human-in-the-Loop /validar), inventario.py, dashboard.py (viewer-friendly). | `db/esquema_bd.sql`, `backend/main.py`, `backend/core/*`, `backend/models/schemas.py`, `backend/routers/*`, `backend/requirements.txt` | **Listo para revisión Gemini — Pendiente Fase 2** |
| 2026-04-11 | Claude | **Cambio Arquitectónico Radical — Plan Maestro v4.0 Final.** Por decisión del PM se redefinió la arquitectura completa: (1) App Principal instalada en cada PC operador como único punto de escritura (CRUD completo + Ollama/Gemma 4 bundleado en el instalador). (2) Accesos Remotos web/móvil como solo lectura: dashboards, reportes y balances sin capacidad de registrar datos. (3) PostgreSQL/Supabase como fuente de verdad central. (4) Ollama + Gemma 4 (E2B para chatbot, E4B para OCR) instalado automáticamente con la App — el usuario nunca toca una terminal. (5) Gemini API como fallback opcional de alta precisión. (6) Online-first para operadores con cola SQLite simple si cae internet — sin sync bidireccional compleja. (7) Roles: admin/operador (escritura) vs viewer (solo lectura, RLS en Supabase). Se eliminaron: sync_queue bidireccional, WebGPU/Transformers.js, resolución de conflictos LWW, tabla dispositivos. PDF ejecutivo v4.0 generado (`Documento_Maestro_ERP_v4.pdf`) con diagrama de flujo creativo de 3 columnas. Plan Maestro actualizado a v4.0. | `BITACORA_COLABORATIVA.md`, `generar_pdf_v3.py`, `Documento_Maestro_ERP_v4.pdf`, Plan Maestro interno v4.0 | **Revisado y Aprobado por Gemini** |
| 2026-04-11 | Gemini | **Consolidación Arquitectónica v4.1 Final.** El PM aprobó el esquema de App Escritorio (Escritura) + App Web (Lectura). Se actualiza el roadmap para inicio inmediato de Fase 1. | `Proyecto_Final_Arquitectura_Universal.md` | **TERMINADA ETAPA DE PLANIFICACIÓN** |
| 2026-04-11 | Gemini | **Ajuste de UI/UX.** Por orden del PM, se elimina definitivamente la funcionalidad de comandos de voz. El sistema se centrará en una interfaz visual moderna y el asistente por chat/texto. | `implementation_plan_ui_ux.md`, `BITACORA_COLABORATIVA.md` | **Revisado y Aprobado por Claude** |
| 2026-04-11 | Claude | **Revisión Arquitectura UI v4.1 + Aclaraciones de Alcance.** (1) Revisión de Milano Software: empresa paraguaya, tablas DBF revelan módulos no contemplados (Banco, Plan de Cuentas, Flujo de Caja, Timbrado). (2) **Aclaración crítica del PM:** El sistema NO emitirá facturas propias. Solo procesará y registrará facturas externas (compra/venta) ingresadas a mano o via OCR. (3) `numero_comprobante` = número del documento externo recibido, no generado por el sistema. (4) Módulos UI propuestos: Comprobantes, Cuentas Corrientes, Inventario, Banco, Plan de Cuentas, Asistente IA, Configuración. Sin comandos de voz — solo chat/texto. **EXPANSIONES FUTURAS PENDIENTES (por orden PM):** Emisión propia de facturas con Timbrado DNIT, Formularios tributarios SET (Form 101, 104, 108), Numeración automática de comprobantes propios. Estas funcionalidades están diseñadas para NO romper la arquitectura actual al incorporarse — el esquema SQL las contempla como campos opcionales desde el inicio. | `BITACORA_COLABORATIVA.md` | **APROBADO POR PM — Listo para iniciar Fase 1** |
| 2026-04-12 | Gemini | **Depuración de Prototipos Obsoletos.** Se movieron archivos de la versión inicial (SQLite/Scripts de prueba) a la carpeta `archive/` para consolidar el entorno de desarrollo v4.1. | `archive/*` | **Entorno Limpio — Listo para Fase 2** |
| 2026-04-12 | Claude | **FASE 2 COMPLETADA — App Principal Frontend + Electron.** (ver detalle arriba) Entregados todos los archivos del frontend Next.js 14 + Electron: (1) **Configuración base**: `package.json` (Next 14 + Electron 33 + electron-builder), `next.config.js`, `tailwind.config.ts`, `tsconfig.json`. (2) **Sistema de tipos y estado**: `src/lib/types.ts` (todos los tipos con montos en string), `src/lib/auth.ts` (Zustand persistido), `src/lib/api.ts` (axios + cola offline automática en ERR_NETWORK), `src/lib/offline.ts` (Dexie.js IndexedDB, auto-sync al reconectar). (3) **Layout y estructura**: `src/app/layout.tsx`, `src/app/providers.tsx` (TanStack Query), `src/app/page.tsx` (redirect a /dashboard), `src/app/(app)/layout.tsx` (auth guard). (4) **Componentes**: `src/components/Sidebar.tsx` (nav + indicador conexión + badge cola offline). (5) **Páginas**: `login/page.tsx`, `dashboard/page.tsx` (4 KPIs + top cuentas corrientes), `comprobantes/page.tsx` + `NuevoComprobanteModal.tsx` (Human-in-the-Loop con aprobar/rechazar), `clientes/page.tsx`, `proveedores/page.tsx`, `inventario/page.tsx` (con alertas stock crítico). (6) **Electron**: `electron/main.js` (BrowserWindow + auto-start Ollama como servicio + Next.js en producción), `electron/preload.js` (contextIsolation API: version, ollamaStatus, ollamaModelos). (7) **Backend completado**: `backend/routers/proveedores.py` (faltaba), `backend/.env`, `frontend/.env.local`. | `frontend/src/**`, `frontend/electron/**`, `backend/routers/proveedores.py`, `backend/.env`, `frontend/.env.local` | **Listo para revisión Gemini — Pendiente Fase 3 (OCR + Dashboard Remoto)** |
| 2026-04-13 | Claude | **LIMPIEZA DE RAÍZ — Archivos movidos a docs/.** Se movieron 7 archivos de documentación histórica desde la raíz a subcarpetas en docs/: (1) **docs/arquitectura/**: Proyecto_Final_Arquitectura_Universal.md. (2) **docs/roadmap/**: Plan_Implementacion_y_Diagrama.md, Propuesta_Ejecutiva_Sistema.md. (3) **docs/especificaciones/**: Estructura_General_Proyecto_Claude.md, Instrucciones_Claude_Roadmap.md. (4) **docs/**: REPORTE_MEJORAS_PROYECTO.md, REPORTE_MEJORAS_ARTEFACTO.md. **Raíz ahora contiene solo 4 archivos:** README.md, ESTRUCTURA_PROYECTO.md, GUIA_RAPIDA.md, BITACORA_COLABORATIVA.md. Estructura profesional y limpia. Commit 0d2d5ab. | `docs/`, raíz limpiada | **Repositorio profesional listo** |
| 2026-04-13 | Claude | **REORGANIZACIÓN ESTRUCTURAL DEL PROYECTO — Fase 3.5.** El proyecto alcanzó complejidad suficiente para necesitar estructura de carpetas profesional. Se crearon: (1) **docs/** con subcarpetas: arquitectura/ (diagramas v4.0, multi-tenant, offline-first), roadmap/ (fases timeline KPIs), especificaciones/ (convenciones datos, IVA, estándares), api/ (API_REFERENCE.md endpoint completo). (2) **config/** para archivos de configuración (next.config.js, tailwind.config.ts, electron-builder.json futuro). (3) **tools/** con scripts/ (setup.sh automatizado). (4) **Archivos raíz nuevos**: README.md (landing page proyecto), ESTRUCTURA_PROYECTO.md (mapa visual), .env.example (template vars), .gitignore (security). (5) **Consolidación documentación**: API_REFERENCE.md duplicado en docs/api, especificaciones en docs/especificaciones, roadmap en docs/roadmap. Alineación con estándares profesionales: separación concerns, fácil navegación, documentación centralizada, scripts reusables. **IMPORTANTE:** Los documentos históricos (Propuesta_Ejecutiva, Proyecto_Final_Arquitectura, Plan_Implementacion) pueden moverse a docs/ cuando sea conveniente. | `docs/`, `config/`, `tools/`, `README.md`, `ESTRUCTURA_PROYECTO.md`, `.env.example`, `.gitignore` | **Estructura profesional lista — Listo para Fase 4** |
| 2026-04-12 | Claude | **FASE 3 COMPLETADA — OCR + Exportación Excel.** Módulos entregados: (1) **Servicio OCR** (`backend/services/ocr.py`): motor dual Ollama Gemma4 local + fallback Gemini API. Prompt de extracción especializado en facturas paraguayas (IVA 0/5/10%, guaraníes). Parser robusto con regex para JSON en markdown. (2) **Router OCR** (`backend/routers/ocr.py`): `GET /ocr/status` (estado motor), `POST /ocr/extraer` (extracción sin crear comprobante), `POST /ocr/procesar` (extracción + creación automática en estado pendiente_revision). Validación MIME + tamaño (20MB). (3) **Servicio Export** (`backend/services/export.py`): generación Excel con openpyxl — estilos profesionales (encabezados azules, bordes, auto-width, formato guaraní). 3 funciones: comprobantes, cuentas corrientes (2 hojas), inventario (con semáforo de stock). (4) **Router Export** (`backend/routers/export.py`): `GET /export/comprobantes`, `/cuentas-corrientes`, `/inventario` — accesible por todos los roles (viewer incluido). StreamingResponse con Content-Disposition. (5) **Frontend OCR** (`frontend/src/app/(app)/ocr/page.tsx`): zona drag-and-drop, preview de imagen, indicador de estado Ollama/Gemini, checkbox forzar Gemini, tabla de ítems extraídos con totales, barra de confianza, botón "Usar datos en nuevo comprobante" que pasa via sessionStorage. (6) **Frontend Export** (`frontend/src/app/(app)/exportar/page.tsx`): 5 opciones de descarga en cards (comprobantes, confirmados, CC, inventario, stock crítico) con estados loading/done. (7) **Integración OCR → Comprobantes**: NuevoComprobanteModal lee datos OCR del sessionStorage y pre-carga formulario con badge "Datos OCR". Comprobantes page abre modal auto si hay datos OCR. (8) **Sidebar actualizado**: nuevos links OCR (solo write) y Exportar. (9) **Deps**: httpx + openpyxl agregados a requirements.txt. | `backend/services/ocr.py`, `backend/services/export.py`, `backend/routers/ocr.py`, `backend/routers/export.py`, `frontend/src/app/(app)/ocr/page.tsx`, `frontend/src/app/(app)/exportar/page.tsx`, `frontend/src/components/Sidebar.tsx`, `frontend/src/app/(app)/comprobantes/*` | **Listo para revisión Gemini — Pendiente Fase 4 (Chatbot IA)** |

| 2026-04-12 | Gemini | **Verificación de Bundle IA.** Se confirma la existencia de `OllamaSetup.exe` en `/resources/ollama`. Se actualizaron las instrucciones para que Claude gestione la instalación silenciosa y el pull automático de modelos. | `Instrucciones_Claude_Roadmap.md` | **Listo para implementación Fase 4** |

| 2026-04-13 | Claude | **CAMBIO DE UBICACIÓN DEL PROYECTO.** Por decisión del PM, la carpeta de trabajo se traslada de `C:\Users\gfcar\Desktop\IA\Empresa 1` a `H:\Mi unidad\Proyectos\Empresa 1 - copia`. Mismo proyecto, mismo repositorio GitHub (`gfcarlos04-del/Proyecto_empresa1`). Se reconstruyó el `.git` en la nueva ubicación (copia tenía corrupción por `desktop.ini` de Windows). Sincronizado con commit limpio de GitHub (`f81b2fc`). **A partir de este momento toda sesión de Claude Code debe abrirse desde `H:\Mi unidad\Proyectos\Empresa 1 - copia`.** | `BITACORA_COLABORATIVA.md` | **Nueva ubicación activa — Listo para continuar Fase 4** |

| 2026-04-15 | Claude | **SESIÓN DE CONFIGURACIÓN — CLAUDE.md + .gitignore.** (1) Se agregó `desktop.ini` al `.gitignore` para limpiar archivos de sistema Windows que aparecían como untracked en toda la estructura de carpetas. (2) Se creó `CLAUDE.md` en la raíz — archivo de instrucciones automáticas para Claude Code que define: equipo (gfcar PM, Gemini Arquitecto, Claude Desarrollador), **protocolo obligatorio de atribución** (registro en bitácora al final de cada turno, firma Co-Authored-By en commits, auditoría cruzada antes de avanzar), estado de fases, reglas técnicas permanentes y archivos clave de referencia. Este archivo se carga automáticamente en cada nueva sesión de Claude Code. (3) Commits: `6d065fc` (.gitignore) y `9555126` (CLAUDE.md). **Nota:** Se decidió suspender push a GitHub temporalmente. Se discutió división de trabajo para cuando se incorpore segunda persona al proyecto. | `.gitignore`, `CLAUDE.md` | **CLAUDE.md activo — Listo para iniciar Fase 4 (Chatbot IA)** |

| 2026-04-15 | Claude | **FASE 4 CÓDIGO COMPLETADO — Chatbot IA (Gemma 4 + Function Calling).** Implementación completa del asistente IA con tool calling: (1) **`backend/services/chatbot.py`**: 7 herramientas en formato OpenAI (`consultar_saldo_cliente`, `consultar_saldo_proveedor`, `consultar_stock`, `listar_comprobantes_pendientes`, `resumen_financiero`, `items_stock_critico`, `buscar_comprobante`), cada una ejecuta una query async real contra la BD. Motor dual: `_chat_ollama()` vía `/v1/chat/completions` con tool calling (hasta 3 rondas), `_chat_gemini()` como fallback. Función `chat()` orquestadora + `verificar_estado()`. SYSTEM_PROMPT en español para contexto ERP paraguayo. (2) **`backend/routers/chatbot.py`**: `POST /chat/mensaje`, `GET /chat/estado`. (3) **`backend/main.py`** actualizado: importa y registra router chatbot. (4) **`frontend/src/lib/api.ts`**: agregado `chatApi` con `enviarMensaje()` y `estado()`. (5) **`frontend/src/components/Sidebar.tsx`**: nuevo nav item "Asistente IA" con ícono Bot. (6) **`frontend/src/app/(app)/asistente/page.tsx`**: UI completa de chat — burbujas user/assistant, indicador de escritura animado, tarjetas expandibles de tool calls (nombre función + args + resultado JSON), 4 botones de acceso rápido, toggle Gemini, indicador motor activo, auto-scroll, Enter envía / Shift+Enter nueva línea, textarea auto-resize. (7) **`backend/.env`**: `DATABASE_URL` sin password (trust auth local), vars Ollama/Gemini. (8) **Scripts de inicio** (`start_backend.bat`, `start_frontend.bat`, `start_all.bat`) con lógica de verificación y arranque correcto. Commit `66696ea`. | `backend/services/chatbot.py`, `backend/routers/chatbot.py`, `backend/main.py`, `frontend/src/lib/api.ts`, `frontend/src/components/Sidebar.tsx`, `frontend/src/app/(app)/asistente/page.tsx`, `start_backend.bat`, `start_frontend.bat`, `start_all.bat` | **Código completo — BLOQUEADO: problema de arranque (ver abajo)** |

| 2026-04-15 | Claude | **PROBLEMA DE ARRANQUE DOCUMENTADO — Google Drive + node_modules.** Se identificaron y resolvieron 3 problemas de arranque: **(A) Backend — imports relativos:** `main.py` usa `from .core.config import settings` (import relativo). Para funcionar, uvicorn debe ejecutarse desde la raíz del proyecto como `python -m uvicorn backend.main:app --port 8000` con Python 3.13 (`C:\Users\gfcar\AppData\Local\Programs\Python\Python313\python.exe`). NO funciona desde dentro de `backend/`. **(B) Frontend — Google Drive corrompe node_modules:** H: es Google Drive File Stream (volumen virtual, no NTFS local). Cuando npm extrae paquetes, Google Drive intercepta las escrituras simultáneas causando `EBADF: bad file descriptor` — archivos críticos quedan en 0 bytes (ej: `next/dist/bin/next` = 0 bytes). Además `mklink /J` falla con "Se requieren volúmenes NTFS locales". **SOLUCIÓN APLICADA:** instalar `node_modules` en `C:\erp_deps\frontend\node_modules` (unidad local, sin sync). El install en C: completó exitosamente: 404 paquetes + `.bin/next.cmd` con 323 bytes reales. Los scripts `start_frontend.bat` y `start_all.bat` usan `C:\erp_deps\frontend\node_modules\.bin\next.cmd` para arrancar Next.js apuntando al código fuente en H:. **(C) Python múltiple:** `python3` en PATH resuelve a Windows Store Python 3.12 (sin uvicorn). El correcto es Python 3.13 en `C:\Users\gfcar\AppData\Local\Programs\Python\Python313\python.exe`. Los scripts `.bat` ya apuntan a esta ruta explícita. **Estado al cierre:** node_modules en C: listo, scripts creados. Pendiente verificar arranque completo en próxima sesión. | `start_backend.bat`, `start_frontend.bat`, `start_all.bat`, `C:\erp_deps\frontend\node_modules\` (local, no en repo) | **⚠️ PRÓXIMA SESIÓN: ejecutar `start_all.bat` y verificar ambos servidores. Si falla, revisar sección B de este registro.** |

| 2026-04-16 | Claude | **VERIFICACIÓN DE ARRANQUE — FASE 4 OPERATIVA.** Se resolvieron los últimos bloqueantes de startup: (1) `requirements.txt` corregido: `asyncpg==0.29.0` → `0.30.0` (sin wheel para Python 3.13 en 0.29.0). (2) `email-validator==2.3.0` agregado a `requirements.txt` (requerido por `pydantic[email]` para `EmailStr` en schemas). (3) PostgreSQL 17 iniciado manualmente (servicio `postgresql-x64-17` estaba detenido). (4) Backend FastAPI verificado operativo: `http://127.0.0.1:8000` retorna `{"sistema":"ERP Universal","version":"4.0.0","estado":"operativo"}` — HTTP 200. (5) `/docs` Swagger accesible. **ESTADO ACTUAL: Backend 100% operativo con PostgreSQL + todos los routers cargados (auth, clientes, proveedores, comprobantes, inventario, dashboard, ocr, export, chatbot). Pendiente: verificar frontend :3000 y chatbot con Ollama.** | `backend/requirements.txt` | **⚠️ PRÓXIMA ACCIÓN: (1) Iniciar PostgreSQL al arrancar Windows (configurar servicio como automático). (2) Ejecutar `start_all.bat` y verificar frontend en :3000. (3) Verificar Ollama con gemma4:2b y gemma4:4b. (4) Test completo del chatbot en /asistente.** |

| 2026-04-16 | Claude | **AUDITORÍA DE SESIÓN + LIMPIEZA GENERAL.** Retorno a `C:\Users\gfcar\Desktop\IA\Empresa 1` por contratiempos con `H:\`. Auditado estado completo: Fase 4 código confirmado presente (chatbot.py, router chatbot, asistente/page.tsx, chatApi en api.ts, router registrado en main.py). Limpieza de archivos sobrantes: eliminados `Proyectos.lnk` (acceso directo Windows), `archivo de prueba fabri/` (carpeta vacía de test), todos los `desktop.ini` en el árbol de carpetas, todos los `__pycache__`. Actualizado `.gitignore` para excluir `desktop.ini`, `*.lnk`, `Thumbs.db` en el futuro. Actualizado `CLAUDE.md`: ubicación activa corregida a `C:\Users\gfcar\Desktop\IA\Empresa 1`, Fase 4 marcada como código completo pendiente verificación de arranque. **Nota de infraestructura (sesión anterior):** `node_modules` reside en `C:\erp_deps\frontend\node_modules` (disco local) debido a que `H:` es Google Drive File Stream y corrompe archivos al sincronizar. Los scripts `.bat` usan esa ruta. | `.gitignore`, `CLAUDE.md`, eliminados archivos basura | **Listo para verificar arranque y completar Fase 4** |
| 2026-04-17 | Claude | **SESIÓN COMPLETA DE ESTABILIZACIÓN — Tailwind CSS + OCR + Chatbot + Build Producción.** (1) **Tailwind CSS fix:** creado `frontend/postcss.config.js` (faltaba — raíz del problema CSS). (2) **Diseño enterprise:** `globals.css` con biblioteca de componentes (card, kpi-card, btn-primary, btn-ghost, badges), paleta SAP/Odoo. (3) **OCR reescrito** (`backend/services/ocr.py`): PyMuPDF para extracción de texto de PDFs digitales (instantáneo, sin IA), model swapping (`keep_alive:0`) para liberar gemma3:1b antes de cargar gemma3:4b con `num_ctx:512`, retorno `manual_requerido` para PDFs imagen. Endpoint `/ocr/preview` para renderizar PDFs como imágenes PNG. (4) **Chatbot fix** (`backend/services/chatbot.py`): fallback `_chat_ollama_sin_tools()` cuando gemma3:1b retorna 400 "does not support tools" — inyecta resumen financiero real en system prompt. (5) **Frontend OCR** (`frontend/src/app/(app)/ocr/page.tsx`): modo Automático + Ingreso Manual, selector tipo (venta/compra/servicio), drag-drop con preview PDF→PNG, barra de progreso animada, manejo graceful de `motor_usado:manual_requerido`. (6) **Sidebar.tsx fix:** type guard `filter((item): item is NavItem => Boolean(item))` para TypeScript. (7) **Build producción:** resuelto conflicto de dos-React entre `frontend/node_modules` y `C:\erp_deps\frontend\node_modules`. Solución final: borrar paquetes duplicados de local + junction de Windows `C:\Users\gfcar\Desktop\IA\Empresa 1\node_modules` → `C:\erp_deps\frontend\node_modules` para que webpack resuelva desde el directorio padre. **Build exitoso:** 13 páginas compiladas, First Load JS: 87.2kB. (8) **INICIAR_ERP.vbs**: cambiado `next dev` → `next start` (producción, -450MB RAM). Config: `OLLAMA_OCR_MODEL=gemma3:4b`, `OLLAMA_CHAT_MODEL=gemma3:1b`. | `frontend/postcss.config.js`, `frontend/src/app/globals.css`, `backend/services/ocr.py`, `backend/services/chatbot.py`, `backend/routers/ocr.py`, `frontend/src/app/(app)/ocr/page.tsx`, `frontend/src/components/Sidebar.tsx`, `frontend/next.config.js`, `INICIAR_ERP.vbs`, junction `Empresa 1\node_modules` | **Build producción ✓ — Listo para arrancar y probar todos los módulos. Pendiente Fase 4b (Electron installer).** |

---

## Tareas Pendientes Prioritarias
1.  [x] ~~**Fase 3:** Módulo OCR con Gemma 4 local + Fallback Gemini.~~ ✅ Completada
2.  [x] ~~**Fase 3:** Exportación profesional a Excel.~~ ✅ Completada
3.  [x] ~~**Fase 3.5:** Reorganización estructural de carpetas + documentación centralizada.~~ ✅ Completada
4.  [x] ~~**Fase 4 (código):** Chatbot IA con Gemma 4 + Function Calling.~~ ✅ Código completo
5.  [ ] **Fase 4 (verificación):** Ejecutar `start_all.bat`, verificar frontend en localhost:3000, probar chat con Gemini.
6.  [ ] **Fase 4b:** Build Electron (.exe installer).
7.  [ ] **Fase H (CRÍTICA):** Contabilidad Real — Plan de Cuentas CRUD + Libro Diario (partida doble) + Libro Mayor + Estados Financieros. Ver `docs/AUDITORIA_SISTEMA_CONTABLE.md`.
8.  [ ] **Fase I (ALTA):** Gestión Bancaria y Fiscal — CRUD cuentas banco + Reportes IVA + Aging de saldos + Notas de Crédito vinculadas.
9.  [ ] **Fase J (MEDIA):** Mejoras Avanzadas — Retenciones + Multi-moneda + Presupuestos + Cierre Contable.
10. [ ] **Fase 5:** Dashboard remoto (web build sin Electron) para acceso remoto.
11. [ ] **Fase 6:** Timbrado DNIT + Emisión propia de facturas.
| 2026-04-18 | Claude | **HOTFIX — Respuesta de Gemini truncada + parser robusto a JSON cortado.** Al probar con el PDF real de MP COSMETICS tras Fase 2, el sistema devolvió 502 "No se pudo parsear la respuesta: {...razon_social_emisor\": \"MP". Causa: el nuevo prompt con `confianza_por_campo` + JSON de ejemplo extendido consume más tokens de output; con 2048 max_tokens el JSON se cortaba a mitad de camino en el valor del emisor. **Fix:** (1) `maxOutputTokens: 2048 → 8192` (Gemini 2.5 Flash lo soporta). (2) Agregado `_reparar_json_truncado()` en `services/ocr.py` — parser de rescate que camina el texto rastreando strings y balance de `{}`/`[]`, identifica el último "punto seguro" (después de coma, `}` o `]` fuera de string), recorta ahí y cierra automáticamente los contenedores abiertos. Tolera cierres escalonados `..."ABC", {"XYZ": ...` → devuelve el array con lo que se pudo leer. `_parse_json_response` ahora tiene dos pasadas: parse directo → si falla, repara + parse. (3) Validado con dos casos sintéticos (truncado en valor simple, truncado a mitad de un item): ambos devuelven dict válido con los campos que sí llegaron. **Efecto:** aunque el modelo corte la respuesta, el sistema extrae lo que pudo leer en lugar de devolver 502. | `backend/services/ocr.py` | **Backend reiniciado. Re-intentar carga de la factura.** |
| 2026-04-18 | Claude | **MEJORA FASE 2 — HITL visual con confianza por campo.** Complementa la Fase 1 (preprocesado OpenCV) con un mecanismo de revisión asistida que baja drásticamente el costo de verificación manual. **Backend** (`backend/services/ocr.py`): (1) SYSTEM_PROMPT ampliado: Gemini ahora debe devolver además de `confianza` (promedio) un objeto `confianza_por_campo` con scores individuales (0.0–1.0) para cada campo que extrajo. Instruido explícitamente: usar < 0.7 para campos dudosos, no incluir null. Ejemplo de salida completo agregado. (2) `_normalizar()` valida que `confianza_por_campo` sea dict de floats en [0,1] — descarta claves malformadas silenciosamente (tolerante a respuestas ruidosas de la IA). (3) Fallback regex y `_sin_clave()` devuelven `confianza_por_campo: {}` para tipo consistente. **Frontend** (`frontend/src/app/(app)/ocr/page.tsx`): (1) `DatoFactura` extiende con `confianza_por_campo?: Record<string, number>`. (2) Constante `UMBRAL_HITL = 0.7` + helper `claseConfianza(campo, conf)` → devuelve `ring-2 ring-red-400 bg-red-50` (<0.5), `ring-2 ring-amber-400 bg-amber-50` (<0.7), vacío (≥0.7). (3) Los 6 inputs principales (numero_comprobante, fecha_emision, ruc_emisor, razon_social_emisor, ruc_cliente, razon_social_cliente) aplican la clase condicional + `title` con porcentaje exacto en hover. (4) Banner HITL nuevo sobre el formulario: si hay campos con confianza < 0.7 muestra "`N` campos necesitan revisión manual" con icono `AlertTriangle` amarillo. **Efecto operativo:** el usuario ahora sabe de un vistazo qué leer dos veces y qué aceptar con confianza. Reduce tiempo de revisión al eliminar la verificación ciega campo por campo. **Verificación:** `npx tsc --noEmit` sin errores, backend reiniciado, `/ocr/confirmar` sigue en el spec, prompt actualizado activo. | `backend/services/ocr.py`, `frontend/src/app/(app)/ocr/page.tsx` | **Fase 1 + Fase 2 listas. Pendiente: prueba real del PM con factura MP COSMETICS para medir si el preprocesado + HITL mejoran la precisión observada.** |
| 2026-04-18 | Claude | **MEJORA FASE 1 — Preprocesado de imagen OpenCV antes de Gemini Vision.** Tras evaluar un abanico de propuestas del PM (Mistral OCR 3, Donut, PaddleOCR-VL, TrOCR, MCPs Apify/Docling, HITL), se decidió NO pivotar el stack (Gemini 2.5 Flash ya cuesta ~$0.00002/factura y lee manuscrito bien) y en cambio agregar la pieza con leverage real: **limpieza de imagen antes del modelo**. Entregado: (1) `backend/services/preprocesado.py` — nuevo módulo con pipeline `mejorar_imagen(bytes) → bytes`: convierte a gris, aplica **deskew** (rotación automática con `cv2.minAreaRect` sobre píxeles de tinta, tolerancia ±15° y límite inferior 0.3° para evitar micro-correcciones), **CLAHE** (contrastado adaptativo `clipLimit=2.5, tileGridSize=8x8` — realza trazos de tinta clara sin quemar el papel) y **denoise no-local** (`fastNlMeansDenoising h=10`, limpia compresión JPEG y sombras típicas de CamScanner sin borrar detalle fino de letras). Tolerante a fallos: cualquier excepción devuelve bytes originales. (2) `backend/services/ocr.py` refactorizado: `_pdf_a_imagen_base64` renombrado a `_pdf_a_png_bytes` (devuelve bytes PNG sin codificar). Ambos paths (PDF rasterizado + imagen directa) pasan por `preprocesado.mejorar_imagen()` antes de codificar a base64 y enviar a Gemini. (3) `backend/requirements.txt`: `opencv-python-headless==4.13.0.92` + `numpy==2.2.2`. Sin GPU, overhead ~150ms por factura A4. (4) Verificación: módulo importa limpio, pipeline testeado con imagen sintética rotada 7° (21947 → 13714 bytes, diferente al original = transformó), backend arranca ok, `/ocr/confirmar` sigue en el spec. **Racional de las decisiones rechazadas:** Mistral OCR 3 = 50× más caro que Gemini 2.5 Flash para nuestro uso. Donut/TrOCR = requieren 50-100 facturas etiquetadas + fine-tuning (semanas de ingeniería). PaddleOCR local = sin GPU en PCs paraguayos da 15-30s por factura. MCP = capa innecesaria, esto es un backend HTTP. | `backend/services/preprocesado.py` (NUEVO), `backend/services/ocr.py`, `backend/requirements.txt` | **Fase 1 verificada — sigue Fase 2 (HITL visual con umbral de confianza en UI).** |
| 2026-04-18 | Claude | **BUGS LATENTES ENCONTRADOS Y ARREGLADOS — /ocr/confirmar ahora funciona end-to-end.** Tras la reparación magistral del día anterior, validación real con `curl` reveló tres defectos que bloqueaban el guardado: (1) **`metodo_carga` inválido**: el schema tiene `CHECK (metodo_carga IN ('manual','ocr_pdf','ocr_imagen'))` pero el código insertaba `'carga_imagen'`/`'carga_pdf'` → violación de constraint. Corregido en `/ocr/confirmar` y `/ocr/procesar`. (2) **asyncpg exige `datetime.date` para DATE, no `str`**: `'2025-12-12'` crasheaba con `'str' object has no attribute 'toordinal'`. Agregado `datetime.strptime(..., "%Y-%m-%d").date()` con HTTPException 422 si el formato es inválido; en `/ocr/procesar` se usa `date.today()` como fallback cuando la IA no logró leer la fecha. (3) **Fallback regex → Gemini Vision defectuoso**: en `services/ocr.py`, si el PDF tenía cualquier capa de texto (CamScanner la agrega con el formulario preimpreso: "Nombre o Razón Social", columnas "CÓDIGO/CANT.", etc.), el sistema entraba al parser regex que jamás lee manuscritos → devolvía cliente/items vacíos. Nueva heurística: el regex sólo gana si extrajo `numero_comprobante` + `monto_total > 0` + `ruc_emisor`; si falta alguno, cae a Gemini Vision con la imagen renderizada. **Validación end-to-end con curl**: (a) Venta MP COSMETICS → FARMA SA → cliente auto-creado (UUID `2bd0f...`) + comprobante `pendiente_revision` + 1 item en detalle ✅. (b) Compra DISTRIBUIDORA PARAGUAY → proveedor auto-creado + comprobante + detalle ✅. Data de prueba limpiada. **Estado:** backend reiniciado en :8000 con todos los fixes, frontend :3000 operativo, `/ocr/confirmar` y `/ocr/extraer` verificados, clave Gemini persistida en `.env`. El flujo completo (subir factura → extraer → revisar → guardar) ya no tiene bloqueantes conocidos. | `backend/routers/ocr.py`, `backend/services/ocr.py` | **Listo para prueba real del PM con el PDF de MP COSMETICS. Próxima sesión: confirmar calidad de extracción y, si hay ajustes, refinar el prompt.** |
| 2026-04-17 | Claude | **REPARACIÓN MAGISTRAL — Flujo OCR → Comprobante de extremo a extremo.** Se rediseñó completamente el flujo de carga automática de facturas paraguayas (preimpresas con campos manuscritos). **Backend:** (1) `backend/core/key_store.py` + `backend/routers/configuracion.py` ya existían: la clave Gemini se guarda una vez en `.env` (`_persistir_en_env`) y vive en memoria del servidor — nunca viaja en cada request. (2) `backend/services/ocr.py`: migrado el modelo a `gemini-2.5-flash:generateContent` (único disponible y más capaz tras probar la lista vía ListModels). `maxOutputTokens: 2048`, sin `responseMimeType` para máxima compatibilidad. SYSTEM_PROMPT reescrito con explicación estructural del comprobante paraguayo (emisor preimpreso arriba, cliente manuscrito, items manuscritos, IVA 0/5/10, total en guaraníes enteros), reglas de lectura de manuscrito (1 vs 7, 0 vs 6, 4 con trazo abierto), validación cruzada (monto en letras vs números, Σitems vs total), ejemplo MP COSMETICS → FARMA SA. `_parse_json_response()` robusto: elimina fences markdown, busca `{` inicial y `rfind("}")` final (antes el regex `\{[\s\S]*?\}` casaba dentro del primer item del array). Campos `ruc_cliente` / `razon_social_cliente` + array `items` agregados al esquema normalizado. (3) `backend/routers/ocr.py`: eliminado el Form field `gemini_key`. Nuevo endpoint `POST /ocr/confirmar` con Pydantic `ItemConfirmar` / `ConfirmarFacturaIn` + helpers `_limpiar_ruc` / `_obtener_tipo_id` / `_resolver_cliente` / `_resolver_proveedor`. Auto-crea cliente (si venta) o proveedor (si compra) por RUC, resuelve `tipo_id` desde "Factura de Venta" / "Factura de Compra", crea comprobante en `pendiente_revision` e inserta `detalle_comprobantes`. **Frontend:** (4) `frontend/src/app/api/ocr/route.ts` + `frontend/src/app/api/ocr/confirmar/route.ts`: proxies mismo-origen Next.js → FastAPI (cero CORS, cero preflight). (5) `frontend/src/lib/api.ts`: interceptor 401 ahora sólo hace logout cuando el detalle menciona `token/autenticado/credenciales` — antes una 401 transitoria al guardar cerraba la sesión. (6) `frontend/src/app/(app)/ocr/page.tsx` reescrito (832 líneas): flujo de una sola página (sin redirect ni sessionStorage), panel Ajustes que guarda la clave en backend, secciones emisor/cliente visualmente resaltadas según tipo (venta→cliente verde / compra→proveedor azul), tabla de items editable con IVA por fila, botón "Recalcular totales" desde items, `guardar()` llama `fetch('/api/ocr/confirmar')` y muestra estado `guardadoOk` con `comprobante_id` + contraparte — opción "Ver comprobantes" (router.push, preserva Zustand) o "Cargar otra". Backend reiniciado, `/ocr/confirmar` verificado en OpenAPI, ambos puertos escuchando (3000/8000). | `backend/routers/ocr.py`, `backend/services/ocr.py`, `backend/routers/configuracion.py`, `backend/core/key_store.py`, `frontend/src/app/api/ocr/route.ts`, `frontend/src/app/api/ocr/confirmar/route.ts`, `frontend/src/app/(app)/ocr/page.tsx`, `frontend/src/lib/api.ts` | **Listo para pruebas end-to-end. Próximo paso: PM re-ingresa la clave Gemini (persiste en .env), sube el PDF de MP COSMETICS y valida que cliente+items+totales queden correctos y el comprobante aparezca en `/comprobantes` sin cerrar sesión.** |
| 2026-04-16 | Claude | **CORRECCIONES CSS + OCR PDF + CHATBOT RAM.** (1) CSS Tailwind no compilaba: causa raiz = falta de postcss.config.js local. SOLUCI�N: instalado tailwindcss/postcss/autoprefixer localmente en frontend/node_modules/ + creado postcss.config.js. CSS ahora compila correctamente, dise�o empresarial visible. (2) globals.css actualizado con @layer components: kpi-card, btn-primary, btn-ghost, badge-confirmado/pendiente/cancelado, page-header, card. (3) OCR 502 Error: Ollama no acepta PDFs directamente. SOLUCI�N: instalado PyMuPDF (pymupdf==1.27.2), agregado a requirements.txt. Funci�n _pdf_a_imagen_base64() convierte primera p�gina del PDF a PNG@200DPI antes de enviarlo a Ollama. (4) Chatbot sin IA: gemma3:4b requiere 4 GB RAM, solo habia 1.2 GB libre (Chrome+Next.js+backend consumen ~6.8 GB). SOLUCI�N: descargado gemma3:1b (815 MB, ~1 GB en RAM). Actualizado .env + config.py: OLLAMA_CHAT_MODEL=gemma3:1b (chatbot liviano), OLLAMA_OCR_MODEL=gemma3:4b (OCR preciso). gemma3:1b verificado funcionando v�a /v1/chat/completions HTTP 200. (5) Contrase�as demo: seed usaba PLACEHOLDER_HASH_BCRYPT. Generado y aplicado bcrypt real para admin@demo.com y operador@demo.com (contrase�a: admin123). (6) Creado INICIAR_ERP.vbs: lanzador de un solo clic, sin ventanas CMD, abre Chrome autom�ticamente. | backend/.env, backend/core/config.py, backend/services/ocr.py, backend/requirements.txt, frontend/postcss.config.js, frontend/src/app/globals.css, INICIAR_ERP.vbs | **PR�XIMA ACCI�N: (1) Probar chatbot en /asistente con gemma3:1b. (2) Probar OCR con PDF real (ya corregido). (3) Probar todos los m�dulos CRUD. (4) Aprobar dise�o frontend. (5) Iniciar Fase 4b (Electron .exe installer).** |

| 2026-04-18 | Claude | **MEJORA DE RUC — normalización + validación de prefijo paraguayo.** Tras feedback del PM: (1) **Formato canónico RUC** — el guión va SIEMPRE y SOLO antes del dígito verificador (último dígito). Ejemplos: "600 22877-4" → "60022877-4", "80138 891-0" → "80138891-0". Agregada función  en  que limpia espacios/guiones y reconstruye con un único guión antes del último dígito. Aplicada tanto en  (path Gemini) como en  (path regex PDF digital). (2) **Validación de prefijo** — :  SIEMPRE arranca con 8 (persona jurídica en PY);  NUNCA arranca con 6 (prefijo inexistente, casi siempre es 0/8 mal leído). Si la validación falla, se baja la confianza del campo a 0.4 → el HITL visual del frontend lo mostrará en amarillo/rojo para que el usuario lo revise. (3) **Prompt Gemini reforzado** — reescritas las reglas 8 y 9 del SYSTEM_PROMPT con ejemplos concretos de formato y los prefijos típicos paraguayos, para que el modelo ya lo tenga en cuenta en la lectura (reduce trabajo del post-procesado). Backend reiniciado en :8000 con los cambios. |  | **Listo para retestear con MP COSMETICS. Siguiente iteración: escuchar qué otros errores detecta el PM para afinar prompt/preprocesado.** |
| 2026-04-18 | Claude | **FASE A del feedback PM — OCR multi-imagen + calidad original + RUC cliente flexible.** (1) **RUC cliente sin validación de prefijo** — el PM aclaró que las PERSONAS pueden tener cualquier dígito inicial; solo las EMPRESAS (jurídicas) arrancan con 8. Removida la heurística que bajaba la confianza cuando el cliente empezaba con 6 (generaba falsos positivos). Solo se mantiene la validación suave para ruc_emisor, que en nuestro contexto (facturas comerciales) siempre es empresa. Prompt Gemini actualizado con la aclaración. (2) **Calidad original preservada** — eliminado el pipeline OpenCV (grayscale+CLAHE+denoise) del flujo por defecto. Ahora las imágenes se envían a Gemini TAL CUAL suben (color intacto, sin procesamiento destructivo). Los PDFs se rasterizan a 300 DPI (antes 250) en RGB. El módulo  queda disponible pero sin llamarse. (3) **Múltiples archivos por factura** — API y UI rediseñadas:  ahora acepta , y  acepta lista de tuplas .  arma un  en UNA sola llamada — Gemini las combina como páginas de la misma factura. Agregado  que rasteriza hasta 5 páginas del PDF. Frontend ():  → ;  con ; drop acepta múltiples; indicador visual "+N archivos más" en la miniatura. Timeout de Gemini subido a 120s para procesar más páginas. | , ,  | **Listo para probar multi-imagen. Siguiente iteración (Fase B): botón Anular, unique constraint (ruc_emisor, numero_comprobante), auto-cálculo de totales con warning si no coinciden con lo extraído.** |
| 2026-04-18 | Claude | **Error 500 al guardar duplicado + cruce codigo-descripcion contra inventario.** (1) **Duplicado como 409** — al guardar una factura con numero ya existente, el schema lanza UniqueViolationError sobre comprobantes_empresa_id_tipo_id_numero_comprobante_key (el constraint de unicidad YA estaba). Antes se escapaba como 500 sin contexto. Ahora en /ocr/confirmar se captura IntegrityError, se hace rollback, y se devuelve 409 con mensaje claro: Ya existe una factura de venta con numero XXX. Cada numero debe ser unico por emisor/tipo. (2) **Cruce codigo-descripcion contra inventario** — nueva funcion _resolver_articulo(db, empresa_id, codigo, descripcion): si el OCR leyo solo el codigo, busca en inventario por codigo y toma la descripcion oficial del catalogo; si leyo solo la descripcion, busca por match exacto o por ILIKE (prefijo de 20 chars) y toma el codigo. Si encuentra match univoco, ademas linkea detalle_comprobantes.inventario_id (ya existia como FK nullable) para trazabilidad. Aplicado en el loop de items antes del INSERT. | backend/routers/ocr.py | **Listo para retest: probar con factura duplicada (debe mostrar mensaje amable) y factura con solo codigo legible (debe autocompletar descripcion si esta en inventario).** |
| 2026-04-18 | Claude | **FASE A refinada — columna Codigo en UI + importacion Excel + prompt mejorado.** (1) **Endpoint GET /ocr/articulo-lookup?codigo=X&descripcion=Y** — reutiliza _resolver_articulo contra la tabla inventario y devuelve el match si existe. Permite autocompletado EN VIVO mientras el operador edita la tabla de items. (2) **Columna Codigo agregada a la tabla de items en OCR page** — ancho 36, editable, con autocompletado al hacer onBlur: si solo hay codigo → busca descripcion del catalogo; si solo hay descripcion → busca codigo. Check verde breve (CheckCircle2) cuando hubo match, fila con bg-emerald-50. TypeScript: nuevo state lookupMatch + funcion lookupArticulo. (3) **Endpoint POST /ocr/importar-excel?tipo_default=venta** — acepta .xlsx con una factura por fila, detecta columnas por nombre con aliases tolerantes a tildes/mayusculas/sinonimos (ej. RUC Cliente = ruccliente = ruc_cliente), parsea fechas en 4 formatos y montos con separadores PY/US, salta duplicados gracefully. Retorna resumen {creadas, duplicadas, errores} + listados detallados. VALIDADO end-to-end: 2 filas con fechas mixtas → 2 creadas; re-import → 2 duplicadas detectadas. (4) **UI importacion Excel** — card emerald con boton Subir Excel en el area de carga, muestra resumen con iconos (creadas/duplicadas/errores) + detalle colapsable. Nuevo proxy Next /api/ocr/importar-excel. (5) **Prompt Gemini refinado** — (a) REGLA DE CODIGOS explicita: EXTRAE codigo Y descripcion independientemente aunque uno este ilegible, el sistema los cruza. (b) Reglas ampliadas de manuscritos paraguayos (1 con gancho, 7 con rayita cruzada, 4 abierto, 3 vs 5). (c) Validacion cruzada aritmetica como fuente de verdad para corregir lecturas dudosas. | backend/routers/ocr.py, backend/services/ocr.py, frontend/src/app/(app)/ocr/page.tsx, frontend/src/app/api/ocr/importar-excel/route.ts | **Listo para retestear: subir factura manuscrita → ver columna Codigo + autocompletado + lectura mejorada. Tambien probar Excel con planilla historica. Plan post: recibos/estado de cuenta por cliente (diferido).** |
| 2026-04-18 | Claude | **Pulido final de IVA 0/5/10 — Paraguay.** Tras recordatorio del PM: (1) **Fallback seguro**: si Gemini devuelve un porcentaje_iva fuera de {0,5,10}, ahora caemos a 0 (exento) en vez de 10 — así no inventamos IVA que no existe. (2) **Prompt ampliado regla 6**: distincion explicita item por columna (EXENTAS → 0, 5% → 5, 10% → 10), aclaracion de que monto_iva_X puede ser 0 aunque haya items (caso factura 100% exenta), formula de subtotal como suma de bases + exentos. (3) **Excel: alias columna Exentas/Exento/monto_exentas/iva0**. El parser ahora lee exentas como columna opcional; si el subtotal no viene explicito, se deduce de total-IVA (los exentos van incluidos ahi). Si solo vienen exentas sueltas, subtotal queda >= exentas. Backend reiniciado y validado. | backend/services/ocr.py, backend/routers/ocr.py | **Fase A cerrada. Siguiente: Fase B (anular comprobante, unique constraint ya existe, auto-calculo de totales con warning de mismatch). El PM descansa.** |
| 2026-04-18 | Claude | **Afinado de OCR tras test con facturas reales (Casa Esperanza, Estudio Contable, ANDE).** Feedback del PM: "las facturas/montos NUNCA deben estar inventados, deben estar sujetos a la aprobacion del usuario". Cambios: (1) **Regla emisor==8 removida** — descubierto con las facturas reales que muchisimos emisores son personas fisicas con negocio (Casa Esperanza RUC 523402-6, Estudio Contable 2157357-3, Imprenta Kike 1158414-9). La regla "ruc_emisor siempre empieza con 8" generaba falsos warnings. `_validar_prefijo_ruc` ahora es no-op (solo valida longitud minima). El prompt Gemini tambien se aclaro: las personas juridicas SUELEN empezar con 8 pero los pequenos comercios/profesionales usan RUC propio con cualquier digito. (2) **Regla 10 prompt — IMAGENES ROTADAS**: instruccion explicita a Gemini de leer sin importar orientacion (la ANDE y varias estan de costado). (3) **Regla 11 — ITEMS SIN PRECIO UNITARIO**: caso super comun en manuscritas (Casa Esperanza: "1 | Campera juvenil | | 135.000") — cuando solo hay subtotal en columna IVA, calcular precio_unitario = subtotal/cantidad. (4) **Regla 12 — NUNCA INVENTES**: instruccion reforzada; mejor null con confianza <0.5 que un valor adivinado. El HITL del frontend lo captura. (5) **Regla 13 — FACTURAS DE SERVICIOS**: honorarios/electricidad con un solo item y descripcion larga, no dividir. (6) **Regla 14 — SERVICIO PUBLICO (ANDE/ESSAP/COPACO)**: numero de factura con formato distinto al XXX-XXX-XXXXXXX, usarlo tal cual; RUCs de empresas publicas conocidos. (7) **CI vs RUC cliente**: si el "RUC/CI" del cliente tiene formato de cedula (1.310.137 sin guion) → es CI, no RUC; dejar ruc_cliente=null y guardar solo el nombre. (8) **Errores de red manejados**: `_extraer_gemini` ahora captura ConnectError/TimeoutException/HTTPStatusError y devuelve `{"error": ...}` con mensaje amigable → el router convierte a 502 con el texto (antes se caia como 500 Internal Server Error). | backend/services/ocr.py | **Afinamiento continuo — esperando proximas facturas de prueba del PM para seguir puliendo el prompt. Fase B (anular + auto-calc con warning) sigue pendiente.** |
| 2026-04-18 | Claude | **Afinado profundo — DESCRIPCION y MONTOS (regla IVA INCLUIDO Paraguay).** PM pidio enfoque en estos dos campos tras probar facturas manuscritas reales (Casa Esperanza, Estudio Contable, ANDE). (1) **DESCRIPCION — 6 reglas nuevas en prompt**: (a) idioma siempre ESPAÑOL PARAGUAYO, NO traducir al ingles (champion != shoe, campera != jacket, buzo != onesie); (b) vocabulario paraguayo listado explicito (textil: conjunto, bóxer algodon, medias, chomba, ojota... + servicios: honorarios profesionales, alquiler, flete, cuota mensual); (c) COMILLAS REPETIDORAS ("") o "id./idem/ídem/do." = copiar descripcion de fila anterior, NO dejar null ni literal; (d) abreviaturas TAL CUAL sin expandir (jvnil no juvenil, alg. no algodon); (e) cantidad al inicio de fila es SEPARADA, no va en descripcion ("2 camperas niños" -> cant=2, desc="camperas niños"); (f) si ilegible sin comillas -> null con baja confianza, no adivinar. (2) **MONTOS — REGLA FUNDAMENTAL IVA INCLUIDO**: aclaracion total en prompt que en Paraguay las columnas "EXENTAS / IVA 5% / IVA 10%" muestran el MONTO BRUTO con IVA adentro, NO el IVA pelado. El IVA real esta en la fila "Liquidacion del IVA" al pie y sale de total × 10/110 (o 5/105). Ejemplo concreto Casa Esperanza: total 835.000 -> iva_10=75.909, subtotal=759.091. Si no hay liquidacion explicita, se calcula segun IVA incluido. (3) **Ejemplo de salida del prompt reescrito con factura real Casa Esperanza** (3 items, items sin precio unitario escrito, CI de cliente en vez de RUC con guion, monto IVA calculado). (4) **Validacion aritmetica en _normalizar**: si cantidad×precio no cuadra con subtotal_item que Gemini devolvio, priorizamos subtotal y recalculamos precio unitario (mas confiable en manuscritas). Si IVAs vienen en 0 pero hay items gravados, se calcula segun IVA incluido y se agrega aviso al array data["warnings"]. Si sumatoria de items discrepa del total >1% se marca con aviso. (5) **UI — panel de avisos aritmeticos azul**: nuevo bloque en OCR page que muestra los warnings del backend con explicacion "IVA incluido Paraguay — ajustalos si la factura muestra otros valores". Tipo DatoFactura extendido con `warnings?: string[]`. (6) Validacion end-to-end: sintaxis Python OK, TSC OK. | backend/services/ocr.py, frontend/src/app/(app)/ocr/page.tsx | **Listo para retest con las mismas facturas: Casa Esperanza deberia extraer "Camperas niños / championes / conjunto buzo" sin traducir, montos con IVA incluido 10%=75.909 correcto. Esperando feedback PM sobre exactitud de descripciones.** |
| 2026-04-18 | Claude | **FASE B + C — Anulacion de comprobantes + Cuentas corrientes (pagos/recibos).** Todo en un turno porque el PM pidio durante Fase B agregar la gestion de saldos/recibos. **FASE B:** (1) **Migracion SQL** `db/migrations/2026-04-18_anulacion_comprobantes.sql`: agrega columnas motivo_anulacion/fecha_anulacion/usuario_anulacion_id y extiende el check de estado_validacion para incluir 'anulado'. Idempotente, aplicada via AsyncSessionLocal. (2) **Endpoint `PATCH /comprobantes/{id}/anular`**: requiere body AnularIn con motivo min 5 chars max 500; soft-anula (no borra), registra usuario+timestamp en columnas dedicadas, rechaza 409 si ya estaba anulado, 404 si no existe. (3) **UI Anular**: boton Ban rojo en cada fila (hidden si ya esta anulado), abre modal con textarea obligatorio + contador de chars + aviso amber "no se elimina pero deja de sumar al saldo" + confirmacion. Estado 'anulado' agregado al filtro y al badge gris. (4) **Duplicados amigables en OCR**: el try/catch de guardar() diferencia ahora 409 (Ya existe...) y 422 (datos invalidos), muestra el detalle del backend con emoji y tono claro. **FASE C (Cuentas Corrientes):** (5) **Router pagos.py completo** en backend: `GET /pagos?comprobante_id` lista pagos con nombre del usuario, `POST /pagos` registra pago (valida saldo > 0, monto <= saldo, rechaza sobre anulados/rechazados), actualiza saldo_pendiente en UPDATE atomico, `DELETE /pagos/{id}` elimina un pago y restituye saldo, `GET /pagos/saldos/clientes`+`/proveedores` usan las vistas v_saldo_clientes/v_saldo_proveedores, `GET /pagos/cliente/{id}/historial` devuelve estado de cuenta detallado (facturas + pagos + totales). Registrado en main.py. (6) **Modal Registrar Pago en UI comprobantes**: boton Wallet verde en filas confirmadas con saldo_pendiente>0, modal con fecha (hoy por default), monto (pre-llenado con el saldo pendiente), medio de pago (efectivo/transferencia/cheque/tarjeta/otro), numero de recibo opcional, notas opcionales; valida monto<=saldo antes de enviar. (7) **Pagina `/cuentas` nueva (Cuentas Corrientes)**: tabs Clientes/Proveedores, tarjeta total por cobrar/por pagar, filtro "solo con saldo pendiente", tabla con total_facturado/total_cobrado(pagado)/saldo_pendiente, estado vacio alegre "Ningun cliente con saldo pendiente 🎉". Agregada al Sidebar como item principal con icono Wallet. (8) api.ts extendido con comprobantesApi.anular() y objeto pagosApi completo. (9) types.ts: estado_validacion incluye 'anulado'. (10) Verificacion: Python AST OK, TypeScript OK, backend arriba, openapi.json lista los 8 endpoints nuevos. | db/migrations/2026-04-18_anulacion_comprobantes.sql (NUEVO), backend/routers/comprobantes.py, backend/routers/pagos.py (NUEVO), backend/main.py, frontend/src/lib/api.ts, frontend/src/lib/types.ts, frontend/src/components/Sidebar.tsx, frontend/src/app/(app)/comprobantes/page.tsx, frontend/src/app/(app)/cuentas/page.tsx (NUEVO), frontend/src/app/(app)/ocr/page.tsx | **Listo para testear: (a) anular factura con motivo corto<5 -> error backend; (b) factura anulada no debe permitir registrar pago; (c) cargar 2 pagos parciales de la misma factura -> saldo va bajando hasta 0; (d) el total de la pagina /cuentas debe coincidir con la suma de saldos_pendientes. Feedback PM proximo turno.** |
| 2026-04-19 | Claude | **FASE E — Adios Ollama + superpoderes admin + auditoria universal + apellido + acceso directo con icono.** PM pidio: (a) eliminar Ollama completamente del stack, (b) admin con poder total incluso borrar toda la base, (c) nombre+apellido en usuarios con todo registrado, (d) cuenta completa por cliente/proveedor, (e) empaquetado con acceso directo con buen diseno. **Backend:** (1) `services/chatbot.py` reescrito: solo Gemini function calling, sin Ollama. `core/config.py` quita OLLAMA_* y agrega `extra='ignore'` para no romper con legacy envs. (2) Migracion `2026-04-18_apellido_usuarios.sql` aplicada: columna `apellido` + backfill tomando segunda palabra del nombre cuando hay espacio. (3) `services/audit.py` NUEVO: helper `registrar()` no bloqueante que inserta en auditoria_log (JSONB datos_anteriores/datos_nuevos, _jsonable serializa Decimal/date/UUID). Cableado en clientes/proveedores/comprobantes/pagos/usuarios/auth para INSERT/UPDATE/DELETE/ANULAR/login. (4) `routers/admin_sistema.py` NUEVO: `GET /admin/stats` (conteos), `GET /admin/auditoria` (filtrable por tabla/accion/usuario/fecha, JOIN a usuarios para nombre+apellido+email), `POST /admin/wipe-datos` (requiere confirmacion='BORRAR TODO', borra pagos+detalle+comprobantes; opcionalmente clientes/proveedores/inventario), `DELETE /admin/comprobantes/{id}` (hard delete con cascada), `GET /admin/backup` (dump JSON completo filtrado por empresa_id). Todos bajo `require_admin` en `core/security.py`. (5) `routers/usuarios.py` extendido con apellido en create/update + `DELETE /usuarios/{id}` con auto-proteccion. (6) `routers/auth.py` en login: actualiza `ultimo_acceso`, audita evento, devuelve `usuario_id`+`usuario_apellido`. **Frontend:** (7) `Sidebar.tsx`: nuevos links /admin/sistema (ServerCog) y /admin/auditoria (History), nombre completo visible con apellido. (8) `/admin/usuarios` con columna apellido, input apellido en modal crear, boton eliminar (Trash2) con confirm. (9) `/admin/sistema` NUEVO: 7 KPI cards (usuarios/clientes/proveedores/comprobantes/pagos/inventario/auditoria), boton descargar backup JSON (Blob), zona de peligro roja con modal "BORRAR TODO" + checkbox incluir catalogos. (10) `/admin/auditoria` NUEVO: filtros tabla/accion/desde/hasta/limite, tabla con badges color por accion (INSERT emerald, UPDATE blue, DELETE red, ANULAR amber, IA_ACTION purple, WIPE red-dark), modal detalle con JSON anteriores/nuevos. (11) `lib/api.ts`: `adminApi` completo + `usuariosApi.eliminar`. (12) Asistente: removidas todas las branches ollama_* de la UI — motorActivo ahora solo gemini_solo/sin_ia. **Empaquetado:** (13) `tools/generar_icono.py`: genera `frontend/public/icon.ico` (7 tamanos 16-256) + `icon.png` (512) con diseno propio — fondo slate-900 redondeado, bloque azul-700 con 3 barras blancas estilo grafico + linea acento blue-400. (14) `tools/CREAR_ACCESO_DIRECTO.ps1` + `INSTALAR_ACCESO_DIRECTO.bat`: crean "ERP Universal.lnk" en Escritorio apuntando a `INICIAR_ERP.vbs` con IconLocation al .ico generado. Ejecutado YA — shortcut instalado en `C:\Users\gfcar\Desktop\ERP Universal.lnk`. (15) `frontend/package.json`: `extraResources` ollama eliminado, NSIS extendido con `createDesktopShortcut` + `createStartMenuShortcut` + `shortcutName: "ERP Universal"`. `frontend/electron/main.js` reescrito sin codigo ollama. `frontend/resources/ollama/` eliminado. **Verificacion:** Backend :8000 escuchando con `/admin/*` endpoints en OpenAPI, frontend :3000 arriba, icono y shortcut existentes en disco. | backend/services/chatbot.py, backend/core/config.py, backend/core/security.py, backend/services/audit.py (NUEVO), backend/routers/admin_sistema.py (NUEVO), backend/routers/usuarios.py, backend/routers/auth.py, backend/routers/clientes.py, backend/routers/proveedores.py, backend/routers/comprobantes.py, backend/routers/pagos.py, backend/models/schemas.py, backend/main.py, db/migrations/2026-04-18_apellido_usuarios.sql (NUEVO), frontend/src/components/Sidebar.tsx, frontend/src/app/login/page.tsx, frontend/src/app/(app)/admin/usuarios/page.tsx, frontend/src/app/(app)/admin/sistema/page.tsx (NUEVO), frontend/src/app/(app)/admin/auditoria/page.tsx (NUEVO), frontend/src/app/(app)/asistente/page.tsx, frontend/src/lib/api.ts, frontend/src/lib/types.ts, frontend/electron/main.js, frontend/package.json, tools/generar_icono.py (NUEVO), tools/CREAR_ACCESO_DIRECTO.ps1 (NUEVO), INSTALAR_ACCESO_DIRECTO.bat (NUEVO), frontend/public/icon.ico (NUEVO), frontend/public/icon.png (NUEVO) | **Listo para usar: doble click en "ERP Universal" del Escritorio para iniciar todo. Admin puede ir a Administracion > Sistema para ver stats/backup/wipe, y Administracion > Auditoria para revisar toda actividad. Todos los endpoints de escritura dejan rastro en auditoria_log. Ollama desaparecio del codigo y del disco. Siguiente: retest PM end-to-end + decidir si se construye el .exe NSIS (fase 4b) o se deja el shortcut+VBS actual como produccion.** |
| 2026-04-18 | Claude | **FASE D — Dashboard rediseñado + Cuentas detalladas + Admin usuarios + Fix Exportar + Condición contado/crédito.** PM pidió: (a) dashboard mas interesante economico dinamico pero accesible, (b) fix exportar que no sirve, (c) clientes con multiples facturas deben verse en detalle no solo suma total, (d) admin con mayores capacidades (crear cuentas/usuarios), (e) soporte compra al contado. **Backend:** (1) Migracion `2026-04-18_condicion_venta.sql` idempotente: columna `comprobantes.condicion` CHECK IN ('contado','credito'), default 'credito', backfill automatico `contado` cuando saldo_pendiente=0. (2) `dashboard.py` +4 endpoints: `/flujo-mensual?meses=N` (ingresos vs egresos por mes, usa generate_series para rellenar meses vacios), `/top-clientes?limite=N`, `/medios-pago` (distribucion pagos ultimos 90 dias), `/ultimos-comprobantes?limite=N`. (3) `pagos.py`: nuevo `/proveedor/{id}/historial` paralelo al de cliente, ambos incluyen `condicion` y `fecha_vencimiento`, y agregan `nombre` en la respuesta. (4) `usuarios.py` NUEVO router: `GET /usuarios`, `POST /usuarios` (valida email unico + hashea con bcrypt), `PATCH /usuarios/{id}` (nombre/rol/activo/password en un solo endpoint), dependencia `require_admin` que rechaza no-admins con 403, auto-proteccion (admin no puede auto-desactivarse ni auto-despotearse). (5) `comprobantes.py`: INSERT con condicion + saldo_inicial=0 si contado; listar devuelve ahora `contraparte`, `tipo` (venta/compra), `condicion`, `cliente_id`, `proveedor_id` via JOIN a clientes/proveedores. (6) `ocr.py`: `/ocr/confirmar` persiste condicion y ajusta saldo_inicial. (7) `schemas.py`: ComprobanteCreate acepta `condicion: Literal["contado","credito"]`. (8) **Bug crítico exportar Excel encontrado y arreglado:** `_auto_width()` en `services/export.py` crasheaba con `AttributeError: 'MergedCell' object has no attribute 'column_letter'` cuando la primera fila era merged (titulo). Reescrita usando `iter_rows()` con skip de MergedCell + `get_column_letter(cell.column)`. Esto era la verdadera causa del "no sirve" — el endpoint devolvia 500. **Frontend:** (9) Dashboard reescrito con recharts (instalado `npm i recharts`): 4 KPIs con tarjetas gradient + tooltip de contexto, grafico de barras Ingresos/Egresos ultimos 6 meses con formateo compacto (k/M), resumen del mes actual (3 tarjetas: ingresos emerald, egresos rose, flujo neto blue/amber), Top 5 clientes con barras de progreso clickeables al detalle, PieChart de medios de pago, tabla de ultimos comprobantes con badges tipo/estado. (10) Pagina nueva `/cuentas/[tipo]/[id]` (ruta dinamica, tipo=cliente|proveedor): KPIs total_facturado/cobrado-pagado/saldo_pendiente, tabla de facturas con columnas N°/fecha/vencimiento/condicion(badge)/monto/saldo/estado con iconos (CheckCircle2 cancelada, AlertTriangle parcial, Ban anulada), tabla de cobros/pagos con recibo/medio/notas. Reutiliza endpoints historial. (11) `/cuentas/page.tsx`: filas ahora clicables (cursor-pointer + ChevronRight) → navegan al detalle individual. (12) `/admin/usuarios/page.tsx` NUEVO: tabla de usuarios con select inline para cambio de rol, toggle activar/desactivar con auto-proteccion visual del usuario propio, modal crear usuario (nombre+email+password+rol), modal cambiar password; guard al top — si rol != admin muestra card amber "acceso restringido". (13) `exportar/page.tsx`: error surface completo — extrae el detail del blob JSON camuflado como octet-stream, muestra panel rojo inline por card con el mensaje real del backend (antes solo console.error silencioso). (14) Sidebar: link "/admin/usuarios" filtrado por `rol==admin`, nueva seccion "Administracion". (15) Comprobantes list: columnas Contraparte + Tipo (venta/compra badge) + Condicion (contado sky / credito violet) agregadas. (16) `api.ts` extendido: `dashboardApi` +4 metodos, `pagosApi` +2 historiales, `usuariosApi` nuevo. **Verificación:** `npx tsc --noEmit` limpio, Python AST OK en 7 archivos. Backend auto-recargado por watchfiles, migracion aplicada en DB. | db/migrations/2026-04-18_condicion_venta.sql (NUEVO), backend/routers/dashboard.py, backend/routers/pagos.py, backend/routers/usuarios.py (NUEVO), backend/routers/comprobantes.py, backend/routers/ocr.py, backend/models/schemas.py, backend/services/export.py, backend/main.py, frontend/src/lib/api.ts, frontend/src/components/Sidebar.tsx, frontend/src/app/(app)/dashboard/page.tsx (REESCRITO), frontend/src/app/(app)/cuentas/page.tsx, frontend/src/app/(app)/cuentas/[tipo]/[id]/page.tsx (NUEVO), frontend/src/app/(app)/admin/usuarios/page.tsx (NUEVO), frontend/src/app/(app)/comprobantes/page.tsx, frontend/src/app/(app)/exportar/page.tsx, frontend/package.json (recharts) | **Listo para probar: (1) `/dashboard` con graficos, (2) click en fila de `/cuentas` → detalle individual por cliente/proveedor con lista de todas sus facturas y pagos, (3) `/admin/usuarios` (solo admin) crear nuevo operador/viewer/admin, cambiar rol via select, cambiar contraseña, activar/desactivar, (4) `/exportar` ahora descarga sin error (MergedCell bug resuelto), si algo falla muestra mensaje real en vez de silenciarlo, (5) crear factura con condicion=contado → saldo queda en 0 automatico (compra/venta al contado no aparece en cuentas corrientes). Esperando retest PM.** |
| 2026-04-19 | Claude | **FASE F — Contado/crédito diferenciados + configuracion de empresa con logo + datos personales de usuarios (telefono/cargo).** PM pidio: (a) contado y credito bien distinguidos en toda la UI sin exigir recibo para contado, (b) poder editar datos de empresa con logo personalizado, (c) todos los usuarios con datos personales y email obligatorio. **Contado vs credito:** (1) Migracion `2026-04-19_medio_pago_contado.sql`: nueva columna `comprobantes.medio_pago_contado` VARCHAR(30) CHECK IN (efectivo,transferencia,cheque,tarjeta,otro), backfill efectivo para contado existente. Aplicada via asyncpg raw (SQLAlchemy prepared statements no soportan DO blocks multi-statement). (2) `schemas.py`: ComprobanteCreate acepta medio_pago_contado Literal; ComprobanteRead extendido con medio_pago_contado + condicion + cliente_id/proveedor_id/contraparte/tipo/fecha_vencimiento (FastAPI response_model los filtraba). (3) `comprobantes.py` + `ocr.py`: INSERT incluye medio_pago_contado solo cuando condicion==contado, default efectivo. (4) `pagos.py`: historialCliente/Proveedor devuelven medio_pago_contado por factura. (5) UI OCR: radio condicion + select medio_pago cuando es contado + texto explicativo "al contado no requiere recibo, solo factura y metodo". (6) UI comprobantes list: cell Condicion muestra medio_pago_contado abajo del chip contado; cell Saldo muestra "Cobrado" para contado. (7) UI cuenta detalle: estadoFactura retorna "Cobrada" con icon verde para contado; pila badge + medio_pago_contado capitalized. **Empresa:** (8) Migracion `2026-04-19_empresa_logo_y_usuarios_datos.sql`: `empresas.logo_url` + `usuarios.telefono` + `usuarios.cargo`. Aplicada. (9) Router NUEVO `backend/routers/empresa.py`: GET (todos autenticados), PATCH (solo admin, razon social/ruc/direccion/telefono/email/moneda), POST /logo (multipart png/jpg/webp/svg <=2MB, guarda en resources/logos/<empresa_id>.<ext>), DELETE /logo. Auditoria en cada mutacion. (10) `main.py`: include_router empresa + StaticFiles mount en /static apuntando a resources/. (11) Pagina NUEVA `/admin/empresa`: layout 2 columnas (logo preview con botones subir/quitar + formulario razon social/ruc/moneda/direccion/tel/email). Subida via FormData. (12) `api.ts`: nuevo empresaApi (obtener, actualizar, subirLogo, quitarLogo). **Usuarios datos personales:** (13) `usuarios.py`: UsuarioCreate/Update aceptan telefono+cargo, INSERT/UPDATE/SELECT incluyen columnas. (14) UI /admin/usuarios: modal con inputs Telefono+Cargo (2-col grid) bajo Email (ya era obligatorio); tabla agrega columna Cargo y muestra telefono debajo del email. **Sidebar integrado:** (15) Sidebar muestra logo empresa en header (fetch `/empresa`, fallback icono Building2) + nombre empresa en lugar de "ERP Universal"; nuevo link /admin/empresa (icono Building2) en seccion Administracion antes de Usuarios. | db/migrations/2026-04-19_medio_pago_contado.sql (NUEVO), db/migrations/2026-04-19_empresa_logo_y_usuarios_datos.sql (NUEVO), backend/routers/empresa.py (NUEVO), backend/routers/comprobantes.py, backend/routers/ocr.py, backend/routers/pagos.py, backend/routers/usuarios.py, backend/main.py, backend/models/schemas.py, frontend/src/app/(app)/admin/empresa/page.tsx (NUEVO), frontend/src/app/(app)/admin/usuarios/page.tsx, frontend/src/app/(app)/ocr/page.tsx, frontend/src/app/(app)/comprobantes/page.tsx, frontend/src/app/(app)/cuentas/[tipo]/[id]/page.tsx, frontend/src/components/Sidebar.tsx, frontend/src/lib/api.ts, frontend/src/lib/types.ts | **Listo para probar: (a) crear factura contado -> elegir medio_pago directo, saldo queda 0 y no requiere recibo, aparece "Cobrada" en detalle; (b) Admin > Empresa: editar razon social + subir logo png -> ver logo en Sidebar instantaneo; (c) Admin > Usuarios: crear operador con telefono/cargo, editar existente. Backend ya tiene router empresa cargado; si no reinicio en hot-reload pendiente restart. Fase 4b (build NSIS) sigue diferida.** |
| 2026-04-19 | Claude | **FASE G — Bug recibos resuelto + credito con plazo + ubicacion fisica + adjuntos imagenes.** PM reporto "LOS RECIBOS NO SE REGISTRAN" y pidio: (a) plazo de tiempo estimado para creditos, (b) opcional ubicacion fisica de factura de papel, (c) adjuntar imagenes a facturas y recibos para consulta posterior. **Bug pagos (raiz del fallo de recibos):** (1) `routers/pagos.py` tenia DOS bugs que hacian que todo POST /pagos crashee: audit usaba `data.monto` (no existe) en vez de `data.monto_pagado` -> AttributeError -> rollback silencioso. SELECT de listar JOIN `u.nombre_completo` (columna no existe) -> reemplazado por `TRIM(COALESCE(u.nombre,'') || ' ' || COALESCE(u.apellido,''))`. **Plazo de credito:** (2) `routers/ocr.py` ConfirmarFacturaIn extendido con `fecha_vencimiento` (YYYY-MM-DD opcional) + `plazo_dias` (entero); si viene plazo sin fecha calcula `fecha_emision + timedelta(plazo)`. INSERT de comprobantes persiste fecha_venc resultante. (3) UI OCR: bloque UI plazos (botones preset 7/15/30/45/60/90/120 dias + input fecha manual) visible solo cuando condicion==credito, explicado con texto "fecha estimada de pago". **Ubicacion fisica:** (4) Migracion `2026-04-19_adjuntos_y_ubicacion_fisica.sql`: `comprobantes.ubicacion_fisica VARCHAR(200)` + `pagos.ruta_adjunto VARCHAR(500)`, aplicadas via asyncpg raw. (5) `schemas.py` ComprobanteRead extendido con `ruta_archivo` + `ubicacion_fisica`. (6) `comprobantes.py` listar SELECT extendido con c.ruta_archivo + c.ubicacion_fisica. (7) `ocr.py` ConfirmarFacturaIn acepta `ubicacion_fisica: Optional[str]`, INSERT persiste. (8) UI OCR: input opcional "Ubicacion fisica (archivador/carpeta/caja)" con icon MapPin, se envia en payload. **Adjuntos imagenes:** (9) `routers/adjuntos.py` NUEVO: POST /adjuntos/comprobante/{id} (8MB max, PNG/JPG/WEBP/PDF) guarda en `resources/adjuntos/comprobantes/<id>.<ext>` y actualiza `comprobantes.ruta_archivo`; DELETE quita archivo + nulifica columna. Paralelo para /adjuntos/pago/{id} actualizando `pagos.ruta_adjunto`. Todo con auditoria y multi-tenant via empresa_id. (10) `main.py` include_router(adjuntos) + StaticFiles mount `/static` para servir archivos directo al frontend. (11) `lib/api.ts` nuevo `adjuntosApi` con subir/quitar para ambos tipos. (12) UI OCR success card: subcomponente `AdjuntarImagenFactura` con file input, upload async con feedback OK/err, permite subir factura justo despues de guardar. (13) UI RegistrarPagoModal en `/comprobantes`: nuevo input file opcional, `pagoMutation` encadena POST /pagos + POST /adjuntos/pago/{id} si hay archivo, falla de adjunto no bloquea registro del pago. **Verificacion:** `npx tsc --noEmit` limpio (0 errores), backend restart limpio con openapi exponiendo `/adjuntos/comprobante/{comprobante_id}` + `/adjuntos/pago/{pago_id}`, import module test OK. | backend/routers/pagos.py, backend/routers/ocr.py, backend/routers/comprobantes.py, backend/routers/adjuntos.py (NUEVO), backend/models/schemas.py, backend/main.py, db/migrations/2026-04-19_adjuntos_y_ubicacion_fisica.sql (NUEVO), frontend/src/app/(app)/ocr/page.tsx, frontend/src/app/(app)/comprobantes/page.tsx, frontend/src/lib/api.ts | **Probar: (1) registrar pago de una factura credito -> ahora SI se guarda, y si adjuntas imagen del recibo se almacena; (2) subir factura OCR credito y elegir plazo "30 dias" -> fecha_vencimiento auto calcula; (3) ubicacion fisica "Archivador A - Carpeta Mayo"; (4) luego subir foto de factura fisica y que quede vinculada. Proxima fase sugerida: **Fase H — Visor de adjuntos**: en tabla comprobantes mostrar miniatura/icono de clip si tiene ruta_archivo y modal/lightbox para verla; mismo en historial de pagos por cliente/proveedor. Tambien: arreglo del OCR "Failed to fetch" + key Gemini en backend (ver plan "jiggly-swinging-candle" pendiente).** |
| 2026-04-20 | Gemini | **ARREGLO DOCUMENTAL — Sincronización completa de documentación con estado real post-Fase G.** La documentación había quedado desactualizada respecto al código tras las Fases D-G implementadas por Claude. Cambios ejecutados: (1) **CLAUDE.md** actualizado: Ollama eliminado del stack, Fases D-G + A-C agregadas como completadas, regla 6 actualizada a "Gemini API motor único", referencia a migraciones SQL agregada en archivos clave. (2) **ESTRUCTURA_PROYECTO.md** reescrito completo: refleja los 16 routers actuales (~60 endpoints), 6 migraciones SQL, 5 servicios backend (ocr, export, chatbot, audit, preprocesado), 16+ páginas frontend incluyendo admin (sistema/usuarios/auditoria/empresa), cuentas corrientes con ruta dinámica [tipo]/[id], proxies API Next.js, resources/ con logos y adjuntos, scripts de inicio (VBS/BAT), iconos generados. (3) **Carpetas fantasma eliminadas**: `docs/{arquitectura,roadmap,especificaciones,api}` y `tools/{scripts,utilities}` — carpetas vacías con llaves literales en el nombre, creadas por error de shell bash en Windows (no expandió las llaves). Las carpetas correctas (`docs/arquitectura/`, `docs/roadmap/`, etc.) siguen intactas. (4) **.env.example** limpiado: eliminadas 4 variables Ollama obsoletas (`OLLAMA_BASE_URL`, `OLLAMA_OCR_MODEL`, `OLLAMA_CHAT_MODEL`, `USAR_GEMINI_FALLBACK`), Gemini marcado como "MOTOR IA ÚNICO — obligatorio para OCR y Chatbot". (5) **README.md** actualizado: estado "Fase G Completada — Sistema operativo", tabla de fases con D-G agregadas, sección IA reescrita sin Ollama, stack corregido, inicio rápido con `INICIAR_ERP.vbs`. (6) **GUIA_RAPIDA.md** modernizado: sección Ollama eliminada, Quick Reference sin Ollama, instrucciones de arranque con 3 métodos (VBS/BAT/manual), secciones debugging actualizadas para OCR con Gemini y Chatbot, nueva sección "Configurar clave Gemini". (7) **Fix seguridad menor `backend/routers/adjuntos.py`**: agregada whitelist `TABLAS_PERMITIDAS = {"comprobantes", "pagos"}` con validación en `_verificar_permiso()` antes del f-string SQL — cierra issue #10 de auditoría v2 (previene SQL injection si la función se reutiliza). **Ningún archivo de lógica/funcionalidad fue modificado — solo documentación, metadatos y 1 guardia de seguridad preventiva.** | `CLAUDE.md`, `ESTRUCTURA_PROYECTO.md`, `.env.example`, `README.md`, `GUIA_RAPIDA.md`, `backend/routers/adjuntos.py`, carpetas fantasma eliminadas, `BITACORA_COLABORATIVA.md` | **Documentación sincronizada con código real — Listo para que Claude verifique y apruebe en auditoría cruzada** |
| 2026-04-23 | Claude | **FASE H — Visor de adjuntos (imagenes y PDFs).** PM queria poder *ver* lo que ya se podia subir en Fase G. Construido un componente reutilizable + integracion en las dos tablas donde aparecen adjuntos (comprobantes y cuentas/[tipo]/[id]). **Componente nuevo:** (1) `frontend/src/components/AdjuntoViewer.tsx` NUEVO. Props `{url, label, onReemplazar, onQuitar, compacto}`. Modo `compacto` = icono Paperclip grisaceo si !url, emerald si hay. Modo full = tarjeta con miniatura (img) o icono rojo FileText si PDF. Click -> lightbox modal con backdrop black/75, header con botones "Abrir en nueva pestaña", "Reemplazar" (input file oculto), "Quitar" (confirm), "Cerrar". Contenido: `<iframe src=... h-[85vh]>` para PDF, `<img class="max-w-full max-h-[85vh] object-contain">` para imagenes. Cache-bust con `?v=${Date.now()}` en URLs. Helper `fullUrl()` antepone `NEXT_PUBLIC_API_URL`. **Tipos extendidos:** (2) `frontend/src/lib/types.ts`: interface `Comprobante` gana `ruta_archivo`, `ubicacion_fisica`, `fecha_vencimiento`, `contraparte`, `tipo`, `cliente_id`, `proveedor_id` (ya los devuelve el backend desde Fase D/F/G pero el tipo no los reflejaba). **Comprobantes list:** (3) `frontend/src/app/(app)/comprobantes/page.tsx`: columna "Adj." (w-14) entre Estado y Acciones, colspan vacio subio a 10, celda usa `<AdjuntoViewer compacto url={c.ruta_archivo}>` con `onReemplazar` y `onQuitar` que llaman a `adjuntosApi.{subirComprobante,quitarComprobante}` + `qc.invalidateQueries(['comprobantes'])` — solo cuando `puedeEscribir()`. **Cuenta detalle:** (4) `frontend/src/app/(app)/cuentas/[tipo]/[id]/page.tsx`: interfaces `Factura` y `Pago` ganan `ruta_archivo` y `ruta_adjunto`; ambas tablas suman columna "Adj." con `<AdjuntoViewer compacto>` (solo-lectura aqui para no duplicar acciones). **Historial backend:** (5) `backend/routers/pagos.py` historialCliente y historialProveedor: SELECT de facturas agrega `ruta_archivo` (antes solo pagos traian `ruta_adjunto`). Ahora el front recibe ambos campos en un solo fetch. **OCR page:** (6) `AdjuntarImagenFactura` en `ocr/page.tsx`: cuando el upload fue OK, el bloque de feedback gana boton "Quitar" con confirm que llama `adjuntosApi.quitarComprobante` y limpia el estado local para que el usuario pueda volver a subir. **Verificacion:** `npx tsc --noEmit` limpio (0 errores), backend reiniciado y `/openapi.json` confirma 51 paths con `/adjuntos/comprobante/{id}` + `/adjuntos/pago/{id}` expuestos. | frontend/src/components/AdjuntoViewer.tsx (NUEVO), frontend/src/lib/types.ts, frontend/src/app/(app)/comprobantes/page.tsx, frontend/src/app/(app)/cuentas/[tipo]/[id]/page.tsx, frontend/src/app/(app)/ocr/page.tsx, backend/routers/pagos.py | **Probar: (1) cargar factura PDF desde OCR -> Guardar -> subir su archivo -> recargar `/comprobantes` -> ver icono clip verde en la fila -> click -> iframe con PDF embebido; (2) en el modal: click "Reemplazar" -> subir otra imagen -> preview se actualiza; click "Quitar" -> confirm -> icono vuelve a gris; (3) `/cuentas/cliente/<id>` muestra columna Adj. en la tabla de facturas y en la de cobros — click para ver lightbox. Proxima fase sugerida (del plan): **Fase 4b** (build NSIS .exe con PyInstaller + electron-builder) o **Fase 5** (rutas `/remoto/*` con guard rol=viewer para dashboard solo lectura). Esperando decision PM sobre cual priorizar.** |

| 2026-04-23 | Claude | **FASE 4b — Build NSIS .exe (instalable en cualquier PC Windows).** PM pidio que el ERP sea exportable e instalable en cualquier computadora. Construida la cadena completa PyInstaller (backend) + Next standalone + electron-builder NSIS + primer-arranque con dialog de credenciales. **Backend empaquetable:** (1) `backend/core/paths.py` NUEVO: helpers `resources_dir()`, `adjuntos_dir()`, `logos_dir()` que respetan `ERP_RESOURCES_DIR` (Electron apunta a %APPDATA%/ERP Universal/resources). Fix critico — la logica vieja `Path(__file__).parent.parent.parent/"resources"` apuntaria dentro del .exe read-only al empacar. (2) `backend/core/config.py`: Settings ahora respeta `ERP_ENV_FILE` (Electron apunta a %APPDATA%/ERP Universal/backend.env) con fallback a `backend/.env` en dev. (3) `backend/main.py`, `backend/routers/adjuntos.py`, `backend/routers/empresa.py` migrados a `core.paths.*` — sin paths hardcoded. (4) `backend/launcher.py` NUEVO: entrypoint PyInstaller que arranca `uvicorn.run(app, host=ERP_BACKEND_HOST, port=ERP_BACKEND_PORT)`. (5) `backend.spec` NUEVO: config PyInstaller con `collect_submodules(uvicorn/sqlalchemy/asyncpg/jose)`, `collect_data_files(openpyxl/jose)`, hiddenimports `fitz cv2 numpy bcrypt email_validator`, excludes tkinter/matplotlib/Qt. **Frontend standalone:** (6) `frontend/next.config.js`: `output: 'standalone'` (permanente, no condicional); genera `.next/standalone/server.js` auto-contenido con node_modules minimo y preserva route handlers (`/api/ocr/route.ts`). (7) `frontend/electron/main.js` reescrito: (a) detecta `%APPDATA%/ERP Universal/backend.env`; si falta, abre `config.html` en BrowserWindow para capturar DATABASE_URL + GEMINI_API_KEY + SUPABASE creds; (b) spawn `process.resourcesPath/backend/backend.exe` con ERP_ENV_FILE + ERP_RESOURCES_DIR inyectadas; (c) `waitForPort(8000)`; (d) spawn `node process.resourcesPath/frontend/standalone/server.js` con PORT=3000 + HOSTNAME=127.0.0.1 + ELECTRON_RUN_AS_NODE=1; (e) `loadURL(http://127.0.0.1:3000)`; (f) cleanup en window-all-closed/before-quit. (8) `frontend/electron/preload.js`: elimina stubs Ollama, expone `saveConfig(data)`. (9) `frontend/electron/config.html` NUEVO: form simple con inputs DATABASE_URL (requerido) + GEMINI_API_KEY + SUPABASE_URL/KEY; en submit llama `electronAPI.saveConfig` que escribe `.env` con JWT_SECRET_KEY generado con `crypto.randomBytes(48)`. **electron-builder:** (10) `frontend/package.json` build config: `asar: true`, `extraResources` copia `../dist/backend/**/*` (PyInstaller output) a `resources/backend/`, `.next/standalone/**` a `resources/frontend/standalone/`, `.next/static/**` a `resources/frontend/standalone/.next/static/`, `public/**` a `resources/frontend/standalone/public/`. NSIS: `oneClick:false`, `perMachine:false`, `allowToChangeInstallationDirectory:true`, accesos escritorio + menu inicio. **Pipeline:** (11) `tools/build_exe.py` NUEVO: orquesta `pip install -r backend/requirements.txt + pyinstaller`, `pyinstaller backend.spec --noconfirm`, `npm ci + npm run build` en frontend, `npx electron-builder --win nsis`. Valida cada paso y loguea output size. (12) `INSTALADOR_README.md` NUEVO: documenta arquitectura del paquete, prerrequisitos de build, comando unico, flujo de primer arranque, troubleshooting, limitaciones (Postgres externo requerido, no firmado). **Verificacion estatica:** imports Python consistentes, next.config sintaxis OK, package.json valido. **Falta verificar en runtime:** ejecutar `python tools/build_exe.py` en maquina con Python + Node, probar instalador resultante en Windows limpio. | backend/core/paths.py (NUEVO), backend/core/config.py, backend/main.py, backend/routers/adjuntos.py, backend/routers/empresa.py, backend/launcher.py (NUEVO), backend.spec (NUEVO), frontend/next.config.js, frontend/electron/main.js (REESCRITO), frontend/electron/preload.js, frontend/electron/config.html (NUEVO), frontend/package.json, tools/build_exe.py (NUEVO), INSTALADOR_README.md (NUEVO) | **Siguiente paso: el PM (o Gemini en auditoria) debe ejecutar `python tools/build_exe.py` en una maquina con Python 3.11+ y Node 20+ para generar `frontend/dist/ERP Universal Setup 4.0.0.exe`; despues probarlo en una PC Windows limpia sin dependencias, completar el dialog de primer arranque con una Supabase URL + Gemini key, y verificar que el login, OCR, dashboard y adjuntos funcionan. Si PyInstaller reporta missing hidden imports en runtime (asyncpg/uvicorn pueden necesitar hooks extra segun version), agregarlos al `hiddenimports` de `backend.spec`. Despues sigue Fase 5 (dashboard remoto viewer).** |

| 2026-04-23 | Claude | **FASE 4b — Hardening: auto-migraciones + admin inicial robusto.** Dos fixes criticos antes de distribuir: (1) el esquema_bd.sql siembra `admin@demo.com` con `password_hash='$2b$12$PLACEHOLDER_HASH_BCRYPT'` (no es bcrypt valido, el login nunca funcionaria en DB fresca), (2) no habia auto-aplicacion de migraciones al instalar contra una Supabase vacia. **Auto-migraciones:** (a) `backend/core/migrations.py` NUEVO: crea `schema_migrations(nombre, aplicada_en)`, bootstrapea `esquema_bd.sql` si no existe `empresas`, aplica incrementalmente `db/migrations/*.sql` con una tx por archivo. Localiza SQL en `sys._MEIPASS/db/` (exe) o `<repo>/db/` (dev). (b) `backend/launcher.py`: carga manual del `.env` apuntado por `ERP_ENV_FILE` a `os.environ` (pydantic-settings ignora extras con `extra="ignore"` y no las re-exporta), llama `aplicar_migraciones_sync(settings.DATABASE_URL)` antes de `uvicorn.run`; si falla exit 2. (c) `backend.spec`: datas incluyen `db/esquema_bd.sql` + `db/migrations/*.sql`. **Admin inicial robusto:** (d) `migrations.py._bootstrap_admin()` — despues del seed, si detecta `password_hash='$2b$12$PLACEHOLDER_HASH_BCRYPT'` y el usuario provee `ERP_ADMIN_EMAIL` + `ERP_ADMIN_PASSWORD` (>=6), borra los usuarios placeholder (admin demo + operador demo), opcionalmente renombra empresa con `ERP_EMPRESA_NOMBRE`, y hace upsert del admin real con `bcrypt.hashpw(password, gensalt(12))`. `ON CONFLICT (empresa_id, email)` respeta la unique compuesta del esquema. Idempotente: en boots subsiguientes no toca nada (el placeholder ya no existe). (e) `frontend/electron/config.html`: formulario extendido con seccion "Primer administrador" — nombre empresa, nombre admin, email admin (required), password admin (required, minlength=6) + validacion client-side. (f) `frontend/electron/main.js`: `config:save` ahora escribe `ERP_ADMIN_EMAIL`, `ERP_ADMIN_PASSWORD`, `ERP_ADMIN_NOMBRE`, `ERP_EMPRESA_NOMBRE` al backend.env; el launcher los lee en el primer boot y se consumen solo una vez. (g) `INSTALADOR_README.md` actualizado con el flujo y la nota de que las vars solo tienen efecto una vez. **Verificacion estatica:** Python AST OK en `migrations.py` y `launcher.py`; HTML del config se parsea; `bcrypt` ya esta en requirements.txt (no hace falta agregar deps). | backend/core/migrations.py (NUEVO), backend/launcher.py, backend.spec, frontend/electron/config.html, frontend/electron/main.js, INSTALADOR_README.md | **Runtime pendiente: correr `python tools/build_exe.py`, instalar el .exe en una PC limpia apuntando a una Supabase vacia; verificar que el dialog pide credenciales, que el primer arranque aplica esquema + migraciones, y que se puede loguear con el email/password ingresados. Si en boots subsiguientes se reinstala sin borrar `%APPDATA%`, el admin queda intacto (el placeholder ya no esta).** |

| 2026-04-24 | Claude | **Tests de guardrails del chatbot + backup completo (ZIP con adjuntos).** Ejecucion del plan propuesto #2 y #4. **#2 — Tests chatbot:** (1) `requirements-dev.txt` NUEVO con `pytest==8.3.3` + `pytest-asyncio==0.24.0`. (2) `pytest.ini` NUEVO con `asyncio_mode=auto`, `testpaths=tests`. (3) `tests/conftest.py` NUEVO: fixtures `FakeDB` (captura queries + stubea rows), `FakeAsyncClient` que comparte cola FIFO de respuestas Gemini via `Holder` (fix critico: cada `async with httpx.AsyncClient()` nuevo en el loop de function-calling debe consumir de la misma cola, no una copia), autouse `_reset_key` que setea `FAKE_KEY_12345`, helpers `gemini_text_response()` y `gemini_function_call()`. (4) `tests/test_chatbot_guardrails.py` NUEVO con **22 tests** cubriendo 13 areas: sin API key -> sin_ia sin queries; texto sin tool call se propaga; respuesta vacia -> OFF_TOPIC; error HTTP -> mensaje controlado sin traceback/httpx; `empresa_id` en todas las queries; operador/viewer bloqueados en `listar_usuarios`, admin si; tool desconocida -> error; integridad `TOOLS <-> FUNCIONES` (sin faltantes ni huerfanos); `SYSTEM_PROMPT` contiene reglas criticas ("NUNCA inventes", "fuera del alcance", "Solo puedo consultar", "Nunca menciones tools", "acciones de escritura"); `OFF_TOPIC` texto exacto; errores de tool no filtran traceback/SQL/excepcion textual; `buscar_cliente` sin texto o con whitespace rechaza sin query; limites capeados (50 y 100); flujo completo function-call + text response; loop infinito se corta en 4 iteraciones. **Resultado: 22/22 passed** en ~1.3s. **#4 — Backup completo:** (5) `backend/routers/admin_sistema.py` nuevo endpoint `GET /admin/backup-zip`: dump JSON de todas las tablas filtradas por `empresa_id` (empresas, usuarios, clientes, proveedores, comprobantes, detalle_comprobantes, pagos, inventario, timbrados, configuracion_empresa), recoleccion de ids de comprobantes/pagos para filtrar archivos, genera ZIP en memoria con `datos.json` + `adjuntos/comprobantes/*` + `adjuntos/pagos/*` + `logos/*` + `manifest.txt`, audita la accion, retorna `StreamingResponse` con `application/zip` y `filename=erp_backup_YYYYMMDD_HHMMSS.zip`. Usa `core.paths.adjuntos_dir()` y `logos_dir()` — funciona tanto en dev como en .exe empaquetado. (6) `frontend/src/lib/api.ts`: `adminApi.backupZip()` nuevo con `responseType: 'blob'`. (7) `frontend/src/app/(app)/admin/sistema/page.tsx`: boton primario "Descargar backup completo (ZIP)" con icon `Archive` y loading state `bajandoZip`; JSON queda como alternativa (`btn-ghost`). **Verificacion:** `pytest -q` -> 22/22 passed; AST Python OK en admin_sistema.py; `npx tsc --noEmit` pendiente (frontend). | `requirements-dev.txt` (NUEVO), `pytest.ini` (NUEVO), `tests/__init__.py` (NUEVO), `tests/conftest.py` (NUEVO), `tests/test_chatbot_guardrails.py` (NUEVO), `backend/routers/admin_sistema.py`, `frontend/src/lib/api.ts`, `frontend/src/app/(app)/admin/sistema/page.tsx` | **Listo para probar: (1) `pytest tests/` desde la raiz -> 22 passed; (2) `/admin/sistema` -> click "Descargar backup completo (ZIP)" -> verificar que el zip contiene `datos.json`, `adjuntos/comprobantes/*`, `adjuntos/pagos/*`, `logos/*`, `manifest.txt`. Siguientes sugerencias del plan: Fase 5 (dashboard remoto rol viewer) o proposal #1 (rate-limit + logs del chatbot).** |

| 2026-04-23 | Claude | **Chatbot — hardening anti-divague + cobertura total de datos.** PM pidio que el chatbot nunca divague y tenga pleno conocimiento del sistema. Reescrito `backend/services/chatbot.py` completo. **Anti-divague:** (1) SYSTEM_PROMPT reforzado con 9 reglas absolutas: todo dato sale de una tool (nunca del modelo); si la pregunta no se resuelve con tools, responde exactamente "Eso esta fuera del alcance del asistente. Solo puedo consultar los datos del sistema ERP."; no inventa; no menciona SQL/tools/IDs; no ejecuta acciones de escritura; no opina ni da consejos financieros. (2) Temperature bajada de 0.3 a 0.1, topP 0.8. (3) Modelo actualizado `gemini-2.0-flash` -> `gemini-2.5-flash` (igual que OCR). (4) Errores de tool se devuelven como `{"error": "Error interno: TipoExcepcion"}` sin traceback para no filtrar ruido al modelo. (5) Si la respuesta del modelo viene vacia (candidates [] o parts sin texto), se devuelve el mensaje OFF_TOPIC en vez de string vacio. **Cobertura de datos ampliada de 7 a 19 tools:** (6) Nuevas: `buscar_cliente` (con saldo+contacto+activo), `buscar_proveedor`, `listar_clientes_top` (facturado o deuda), `listar_proveedores_top`, `listar_comprobantes_vencidos` (credito con fecha_vencimiento < hoy, incluye dias_vencido), `ultimos_comprobantes` (filtrable venta/compra), `detalle_comprobante` (items del detalle_comprobantes con precio/iva/subtotal), `historial_contraparte` (facturas + pagos de un cliente/proveedor puntual en un solo fetch), `listar_pagos_recientes`, `listar_inventario` (filtrable por categoria), `flujo_mensual` (ingresos vs egresos con generate_series), `distribucion_medios_pago` (ultimos 90 dias), `info_empresa`, `listar_usuarios` (solo admin — chequeo de rol). Renombradas las viejas para consistencia. **Seguridad:** (7) `listar_usuarios` valida rol admin en el ejecutor (no solo en la tool description). (8) Todos los SELECT estan limitados por `empresa_id` — multi-tenant nunca se rompe. (9) Rol del usuario ahora llega al servicio (extraido de `current_user.get("rol")` en `routers/chatbot.py`). (10) Validaciones de input en cada ejecutor: strip+chequeo de texto vacio, LIMIT capeado a 50 o 100, fallback de tipos. **Compatibilidad:** firma `chat()` agrega `rol` con default "operador" — el frontend no necesita cambios, pero recibe respuestas mejores. | backend/services/chatbot.py (REESCRITO), backend/routers/chatbot.py | **Probar: (1) "¿cual es la capital de Francia?" -> debe responder exactamente "Eso esta fuera del alcance del asistente..."; (2) "saldo de Juan Perez" -> busca cliente, muestra saldo o pide aclarar si hay varios; (3) "facturas vencidas por cobrar" -> lista con dias_vencido; (4) "historial del cliente ACME" -> facturas + cobros; (5) "detalle de la factura 001-001-0001234"; (6) "flujo mensual 12 meses"; (7) como operador: "lista de usuarios" -> error "solo admin"; (8) "borrame todas las facturas" -> responde que no puede hacer acciones de escritura. Si PM quiere mas tools (ej: buscar por RUC exacto, comparativos anio-vs-anio), agregarlas siguiendo el patron.** |

| 2026-04-24 | Claude | **Auditoría cruzada Gemini OK.** Revisé `docs/AUDITORIA_SISTEMA_CONTABLE.md` y `CLAUDE.md` actualizados por Gemini. Hallazgos confirmados: plan_cuentas sin uso, sin libro diario ni estados financieros. Aprobado el diagnóstico. Las fases H/I/J propuestas son correctas técnicamente. Sin ajustes necesarios — se procede directamente con Fase H por aprobación explícita del PM. | `BITACORA_COLABORATIVA.md` | **Aprobado — iniciando Fase H.** |
| 2026-04-24 | Claude | **FASE H — Contabilidad Real: Plan de Cuentas + Libro Diario + Libro Mayor + Reportes Financieros.** PM aprobó la fase completa. Referencia técnica: `docs/especificaciones/REFERENCIAS_CONTABLES_PARAGUAY.md`. **Backend:** (1) `db/migrations/2026-04-24_contabilidad_fase_h.sql` NUEVO: tablas `asientos_contables` (id/empresa_id/numero/fecha/concepto/comprobante_id/pago_id/tipo/usuario_id — UNIQUE empresa+numero) y `detalle_asientos` (id/empresa_id/asiento_id/cuenta_id/debe/haber — CHECK debe>=0, haber>=0, NO puede ambos=0) + 3 cuentas nuevas en plan_cuentas (1.1.04 IVA Crédito Fiscal, 2.1.03 IVA Débito Fiscal, 2.1.04 Anticipos Clientes). (2) `backend/services/contabilidad.py` NUEVO: helper `_codigo_a_id()` (lookup seguro por código+empresa), helper `_insertar_asiento()` (valida partida doble, auto-numeración secuencial, inserta cabecera + partidas), funciones de asiento automático para venta (DEBE CxC / HABER Ventas+IVADébito), compra (DEBE Gastos+IVACrédito / HABER CxP), cobro (DEBE Caja / HABER CxC) y pago a proveedor (DEBE CxP / HABER Caja), `revertir_asiento_comprobante()` (swap debe/haber para anulación), `libro_mayor_cuenta()` (window function SUM para saldo acumulado), `balance_comprobacion()`, `estado_resultados()` y `balance_general()`. (3) `backend/routers/plan_cuentas.py` NUEVO: `GET /plan-cuentas` (árbol completo con saldo acumulado por JOINs; `?solo_hojas=true` para selectores), `POST /plan-cuentas` (solo admin), `PATCH /plan-cuentas/{id}` (solo nombre), `DELETE /plan-cuentas/{id}` (desactiva si es hoja sin movimientos). (4) `backend/routers/contabilidad.py` NUEVO: `GET /contabilidad/libro-diario` (asientos con totales), `GET /contabilidad/libro-diario/{id}` (detalle con partidas + nombres de cuenta), `GET /contabilidad/mayor/{cuenta_id}` (saldo acumulado con window function), `GET /contabilidad/balance-comprobacion`, `GET /contabilidad/estado-resultados`, `GET /contabilidad/balance-general`, `POST /contabilidad/asientos` (asiento manual con validación partida doble en Pydantic `@model_validator`). (5) Hooks en `routers/comprobantes.py`: tras crear comprobante llama `asiento_comprobante_venta/compra()` en try/except no-bloqueante; tras anular llama `revertir_asiento_comprobante()`. (6) Hooks en `routers/pagos.py`: tras registrar pago llama `asiento_cobro/asiento_pago_proveedor()` en try/except. (7) `backend/main.py` registra `plan_cuentas.router` y `contabilidad.router`. **Frontend:** (8) `frontend/src/lib/api.ts` extendido con `planCuentasApi` (listar/crear/editar/desactivar) y `contabilidadApi` (libroDiario/asientoDetalle/libroMayor/balanceComprobacion/estadoResultados/balanceGeneral/crearAsiento). (9) `frontend/src/app/(app)/contabilidad/plan-cuentas/page.tsx` NUEVO: árbol colapsable con `renderCuenta()` recursivo, inline edit de nombre, create form con todos los campos (código/nombre/tipo/naturaleza/padre/es_hoja), desactivar con confirm. (10) `frontend/src/app/(app)/contabilidad/libro-diario/page.tsx` NUEVO: layout dos paneles — lista a la izquierda (filtros desde/hasta/tipo), panel detalle a la derecha al seleccionar fila con tabla partidas (código/cuenta/debe/haber) y total footer. (11) `frontend/src/app/(app)/contabilidad/libro-mayor/page.tsx` NUEVO: selector de cuenta (solo_hojas), rango de fechas, 3 KPIs (Total Debe/Haber/Saldo Final), tabla T-account con saldo acumulado vía window function backend. (12) `frontend/src/app/(app)/contabilidad/reportes/page.tsx` NUEVO: 3 tabs — Balance de Comprobación (tabla árbol con totales cuadra), Estado de Resultados (KPIs 3-col + tablas ingresos/egresos separados + cuadro resultado neto), Balance General (KPIs activo/pasivo/patrimonio + 2 columnas activo vs pasivo+patrimonio + verificación ecuación). (13) `frontend/src/components/Sidebar.tsx` actualizado: nueva sección "Contabilidad" con 4 entradas (BookMarked Plan de Cuentas, BookOpen Libro Diario, BarChart3 Libro Mayor, PieChart Reportes). **Verificación:** `python -c "from backend.main import app; print(len(app.routes))"` → 85 rutas. Frontend compila sin errores TypeScript. | `db/migrations/2026-04-24_contabilidad_fase_h.sql` (NUEVO), `backend/services/contabilidad.py` (NUEVO), `backend/routers/plan_cuentas.py` (NUEVO), `backend/routers/contabilidad.py` (NUEVO), `backend/routers/comprobantes.py`, `backend/routers/pagos.py`, `backend/main.py`, `frontend/src/lib/api.ts`, `frontend/src/components/Sidebar.tsx`, `frontend/src/app/(app)/contabilidad/plan-cuentas/page.tsx` (NUEVO), `frontend/src/app/(app)/contabilidad/libro-diario/page.tsx` (NUEVO), `frontend/src/app/(app)/contabilidad/libro-mayor/page.tsx` (NUEVO), `frontend/src/app/(app)/contabilidad/reportes/page.tsx` (NUEVO) | **Fase H completa. Probar: (1) `/contabilidad/plan-cuentas` → árbol jerárquico colapsable con saldos; (2) crear una factura → ir a `/contabilidad/libro-diario` → debe aparecer el asiento automático; (3) `/contabilidad/libro-mayor` → seleccionar cuenta Clientes → ver saldo acumulado; (4) `/contabilidad/reportes` tab Estado de Resultados → con facturas de venta debe mostrar ingresos. Siguiente fase sugerida: Fase I (Gestión Bancaria + Reportes IVA RG 90 + Aging cuentas por cobrar/pagar + Notas Crédito vinculadas).** |
| 2026-04-24 | Claude | **Movimientos + Actividad + Contraparte + Cargado por (mejoras transversales).** PM pidió: registro visible de cobros/pagos por proveedor, nombres de emisores/imputados en listados, quién actuó en cada acción del sistema. **Movimientos:** (1) `backend/routers/pagos.py` nuevo endpoint `GET /pagos/movimientos`: JOIN comprobantes+clientes+proveedores+usuarios, retorna tipo(cobro/pago), contraparte, RUC, numero_comprobante, numero_recibo, fecha, monto, medio_pago, usuario_nombre + summary {total_cobros, total_pagos, balance}. (2) `frontend/src/app/(app)/movimientos/page.tsx` NUEVO: KPIs 3-col (cobros/pagos/balance), filtros tipo+fechas+busqueda, tabla con links a `/cuentas/[tipo]/[id]`, botón Excel. (3) `frontend/src/lib/api.ts`: `pagosApi.movimientos()`. **Actividad:** (4) `backend/routers/actividad.py` NUEVO: `GET /actividad` accesible todos los roles (viewer incluido), retorna metadatos seguros sin payloads JSONB (fecha/accion_label/tabla_label/usuario_nombre/email), mappings human-readable. (5) `frontend/src/app/(app)/actividad/page.tsx` NUEVO: feed con avatar inicial, badge color por acción (INSERT/UPDATE/DELETE/LOGIN), auto-refresh 30s. (6) `frontend/src/lib/api.ts`: `actividadApi.listar()`. **Contraparte visible:** (7) `backend/routers/comprobantes.py` listar: JOIN usuarios → columna `cargado_por` (nombre+apellido). (8) `frontend/src/app/(app)/comprobantes/page.tsx`: columna "Cargado por" en tabla. **Sidebar:** (9) Links `/movimientos` (ArrowDownUp) y `/actividad` (History) agregados con secciones Principal y Herramientas. `backend/main.py` registra `actividad.router`. | `backend/routers/pagos.py`, `backend/routers/actividad.py` (NUEVO), `backend/routers/comprobantes.py`, `backend/main.py`, `frontend/src/app/(app)/movimientos/page.tsx` (NUEVO), `frontend/src/app/(app)/actividad/page.tsx` (NUEVO), `frontend/src/lib/api.ts`, `frontend/src/components/Sidebar.tsx`, `frontend/src/app/(app)/comprobantes/page.tsx` | **Probar: `/movimientos` muestra cobros+pagos con contraparte; `/actividad` muestra quién actuó en cada evento; comprobantes list muestra columna "Cargado por". Siguiente: Fase I.** |
| 2026-04-24 | Claude | **AUDITORÍA GEMINI — Aprobado.** Revisé entrada Gemini 2026-04-24 (Auditoría contable). Diagnóstico correcto, Fases H/I/J bien estructuradas, documentación `AUDITORIA_SISTEMA_CONTABLE.md` completa. Aprobado sin ajustes. | `BITACORA_COLABORATIVA.md` | **Aprobado.** |

| 2026-04-24 | Claude | **FASE I — Gestión Bancaria y Fiscal (Bancos + IVA RG90 + Aging + NC vinculadas).** **I.1 Cuentas Bancarias:** `backend/routers/cuentas_banco.py` NUEVO con `GET /bancos` (lista + total_movimientos), `POST /bancos` (crea con saldo_inicial → movimiento apertura auto), `PATCH /bancos/{id}`, `GET /bancos/{id}/movimientos` (con saldo acumulado window function), `POST /bancos/{id}/movimientos` (manual: depósito/retiro/comisión, actualiza saldo_actual). `frontend/src/app/(app)/bancos/page.tsx` NUEVO: KPI total, lista de cuentas clickeable, panel detalle con créditos/débitos/saldo, tabla T-account, form registro movimiento manual. **I.2 Reportes IVA (RG 90 DNIT):** `backend/routers/reportes.py` NUEVO con `GET /reportes/iva/ventas` (RUC comprador, N° comprobante, Base 10%/5%, IVA 10%/5%, Exentas, Total — fórmulas IVA incluido PY: IVA10=total/11, IVA5=total/21), `GET /reportes/iva/compras` (Crédito Fiscal), `GET /reportes/iva/liquidacion` (IVA Débito - Crédito = saldo a_pagar/a_favor/neutro), `GET /reportes/aging`. `frontend/src/app/(app)/reportes/iva/page.tsx` NUEVO: 3 tabs (Libro Ventas/Compras/Liquidación), tabla con totales footer, panel liquidación con semáforo visual. **I.3 Antigüedad de Saldos:** `GET /reportes/aging?tipo=clientes|proveedores` — clasifica comprobantes con saldo_pendiente>0 y credito='confirmado' en tramos (corriente/1-30/31-60/61-90/+90 días) usando `CURRENT_DATE - fecha_vencimiento`. `frontend/src/app/(app)/reportes/aging/page.tsx` NUEVO: tarjetas por tramo clickeables (filtra tabla), alerta visual +90 días, tabla completa. **I.4 Notas de Crédito vinculadas:** `db/migrations/2026-04-24_fase_i_bancos_iva_nc.sql` NUEVO: agrega `comprobante_origen_id UUID REFERENCES comprobantes(id)` + índices de performance. `backend/models/schemas.py`: `ComprobanteCreate` gana `comprobante_origen_id`. `backend/routers/comprobantes.py`: al crear NC con origen, verifica que existe el origen; al insertar, guarda `origen_id`; si el NC es crédito, reduce `saldo_pendiente` del origen con `GREATEST(0, saldo_pendiente - monto)`. **Sidebar + api.ts:** Sección "Bancos y Fiscal" con 3 entradas (Landmark, Receipt, Clock). `bancosApi` y `reportesApi` en api.ts. `backend/main.py` registra `cuentas_banco.router` y `reportes.router`. **Verificación:** 94 rutas backend, TypeScript sin errores. | `db/migrations/2026-04-24_fase_i_bancos_iva_nc.sql` (NUEVO), `backend/routers/cuentas_banco.py` (NUEVO), `backend/routers/reportes.py` (NUEVO), `backend/routers/comprobantes.py`, `backend/models/schemas.py`, `backend/main.py`, `frontend/src/app/(app)/bancos/page.tsx` (NUEVO), `frontend/src/app/(app)/reportes/iva/page.tsx` (NUEVO), `frontend/src/app/(app)/reportes/aging/page.tsx` (NUEVO), `frontend/src/components/Sidebar.tsx`, `frontend/src/lib/api.ts` | **Fase I completa. Probar: (1) `/bancos` → crear cuenta con saldo inicial → ver movimiento de apertura; (2) registrar pago manual y ver que actualiza saldo; (3) `/reportes/iva` → seleccionar mes con facturas → verificar IVA10=total/11; (4) `/reportes/aging` → ver facturas vencidas clasificadas por tramo; (5) crear NC con comprobante_origen_id → verificar que reduce saldo del origen. Siguiente: Fase J (Retenciones + Presupuestos + Cierre) o Polish pendiente.** |
| 2026-04-24 | Claude | **BUILD PIPELINE — Fase 4b ejecutada: PyInstaller + Next.js standalone + NSIS installer.** PM solicitó ejecutar el pipeline completo de `tools/build_exe.py` y verificar que todos los routers de contabilidad estén empaquetados. **Paso 1 — PyInstaller 6.20.0:** instalado en Python 3.13, ejecutado con `backend.spec` actualizado (se agregaron `collect_submodules("backend.routers")`, `collect_submodules("backend.services")`, `collect_submodules("backend.core")`, `collect_submodules("backend.models")` para garantizar inclusión completa). Verificación con `pyi-archive_viewer -r -l dist/backend/backend.exe`: confirmados en PYZ embebido `backend.routers.cierre_contable`, `backend.routers.contabilidad`, `backend.routers.cuentas_banco`, `backend.routers.plan_cuentas`, `backend.routers.presupuestos`, `backend.routers.reportes`, `backend.routers.retenciones`, `backend.services.contabilidad`. Las 9 migraciones SQL (incluye Fase H, I, J) confirmadas en `dist/backend/_internal/db/migrations/`. `backend.exe` = 29 MB, `_internal/` = 121 ítems. **Paso 2 — Next.js build standalone:** `npm run build` completado en `frontend/` — 33 rutas compiladas incluyendo `/contabilidad/cierre`, `/contabilidad/presupuestos`, `/contabilidad/retenciones`, `/reportes/iva`, `/reportes/aging`, `/bancos`. Standalone generado en `.next/standalone/server.js`. **Paso 3 — electron-builder NSIS:** Issues resueltos: (a) `icon.ico` regenerado a multi-resolución 16×16→256×256 con Pillow desde `icon.png` 512×512; (b) campo `author` agregado a `package.json`; (c) cache `winCodeSign-2.6.0` pre-extraída manualmente con Python para evitar error de symlinks macOS en 7zip de Windows. Resultado: **`frontend/dist/ERP Universal Setup 4.0.0.exe` — 258 MB**. Firmado: skipped (sin certificado de código — producción normal). `CLAUDE.md` actualizado: Fases H, I, J, 4b marcadas como ✅ Completadas, migraciones actualizadas a 9. | `CLAUDE.md`, `backend.spec`, `frontend/package.json`, `frontend/public/icon.ico` | **Instalador listo en `frontend/dist/ERP Universal Setup 4.0.0.exe`. Para distribuir: copiar ese archivo a la PC destino, doble click → instala en AppData\Local\Programs\ERP Universal, crea acceso directo en escritorio. Al primer arranque pide: DATABASE_URL (Supabase), Gemini Key, email/password del primer admin. Fases siguientes disponibles: 5 (dashboard viewer remoto) o 6 (Timbrado DNIT).** |
| 2026-04-24 | Claude | **FASE J — Retenciones + Presupuestos + Cierre Contable + Sidebar desplegable unificado.** **J.1 Retenciones:** `backend/routers/retenciones.py` NUEVO con `GET /retenciones` (filtros desde/hasta/tipo, totales iva_retenido/renta_retenido/total_retenido), `POST /retenciones` (tipo iva/renta/ambos, porcentaje, monto_base, monto_retenido, fecha, certificado), `DELETE /retenciones/{id}`. `frontend/src/app/(app)/contabilidad/retenciones/page.tsx` NUEVO: 3 KPIs (IVA Retenido/Renta/Total), filtros fecha+tipo, form con auto-cálculo `monto_retenido = base × pct / 100`, tabla con footer totales. **J.2 Presupuestos:** `backend/routers/presupuestos.py` NUEVO con `GET /presupuestos` (lista + total_presupuestado por cuenta), `POST /presupuestos` (nombre, periodo_inicio, periodo_fin, detalle líneas), `PATCH /presupuestos/{id}` (nombre/estado/notas), `GET /presupuestos/{id}/comparativo` (JOIN detalle_presupuesto → plan_cuentas → detalle_asientos → asientos_contables filtrado por rango, devuelve monto_presupuestado/real/diferencia + ejecucion_pct), `DELETE /presupuestos/{id}` (solo borrador). `frontend/src/app/(app)/contabilidad/presupuestos/page.tsx` NUEVO: lista lateral de presupuestos con estado badge (borrador/activo/cerrado), panel derecho con 4 KPIs (presupuestado/real/diferencia/ejecucion%), barra de progreso con semáforo (verde <80%/ámbar 80-99%/rojo ≥100%), tabla comparativo por cuenta con trending icons, botones Activar/Cerrar/Eliminar según estado. **J.3 Cierre Contable:** `backend/routers/cierre_contable.py` NUEVO con `GET /periodos-contables` (estadísticas asientos+comprobantes del período), `POST /periodos-contables` (abrir — solo admin), `PATCH /periodos-contables/{id}/cerrar` (registro fecha_cierre+usuario), `PATCH /periodos-contables/{id}/reabrir`. Helper `verificar_periodo_abierto(db, empresa_id, fecha)` exportado — usado en `routers/comprobantes.py` al crear para bloquear fechas en períodos cerrados (HTTP 409). `frontend/src/app/(app)/contabilidad/cierre/page.tsx` NUEVO: sección "Períodos abiertos" con botón "Cerrar período" (admin), modal de confirmación con notas, tabla "Períodos cerrados" con Reabrir. **J.4 Sidebar desplegable unificado:** Reescritura de `frontend/src/components/Sidebar.tsx`: eliminadas secciones estáticas "Contabilidad" y "Bancos y Fiscal". Reemplazadas por un único botón desplegable "Contabilidad" con ChevronDown rotable, auto-expansión si `pathname` comienza con `/contabilidad`, `/bancos`, `/reportes/iva` o `/reportes/aging`. 10 entradas indentadas con `pl-6` y `text-xs`: Plan de Cuentas, Libro Diario, Libro Mayor, Reportes, Cuentas Bancarias, Libro IVA, Antigüedad Saldos, Retenciones, Presupuestos, Cierre Contable. **api.ts:** Nuevos exports `retencionesApi`, `presupuestosApi`, `cierreContableApi`. **Migración:** `db/migrations/2026-04-24_fase_j_retenciones_presupuestos_cierre.sql` con tablas `retenciones`, `presupuestos`, `detalle_presupuesto`, `periodos_contables` (UNIQUE empresa+periodo). `backend/main.py` registra 3 nuevos routers. | `db/migrations/2026-04-24_fase_j_retenciones_presupuestos_cierre.sql` (NUEVO), `backend/routers/retenciones.py` (NUEVO), `backend/routers/presupuestos.py` (NUEVO), `backend/routers/cierre_contable.py` (NUEVO), `backend/routers/comprobantes.py`, `backend/main.py`, `frontend/src/app/(app)/contabilidad/retenciones/page.tsx` (NUEVO), `frontend/src/app/(app)/contabilidad/presupuestos/page.tsx` (NUEVO), `frontend/src/app/(app)/contabilidad/cierre/page.tsx` (NUEVO), `frontend/src/components/Sidebar.tsx`, `frontend/src/lib/api.ts` | **Fase J completa. Probar: (1) Sidebar → click "Contabilidad" → despliega 10 items; navegar a /bancos → grupo se mantiene expandido; (2) `/contabilidad/retenciones` → crear retención IVA 0.5% con base G.1.000.000 → monto retenido auto-calculado G.5.000; (3) `/contabilidad/presupuestos` → crear presupuesto borrador → activar → ver comparativo 0% ejecución; (4) `/contabilidad/cierre` → abrir período 2026-04 → cerrar → intentar crear comprobante con fecha 2026-04-xx → HTTP 409. Fases siguientes disponibles: Fase 5 (dashboard viewer remoto), Fase 6 (Timbrado DNIT), Polish (paginación real, tests E2E).** |
| 2026-04-24 | Claude | **CORRECCIONES DE INTEGRIDAD POST-AUDITORÍA — 4 fixes críticos.** PM solicitó aplicar hallazgos críticos detectados en auditoría técnica final. **(1) Reversión contable al eliminar pagos:** nueva función `revertir_asiento_pago(db, empresa_id, pago_id, usuario_id)` en `backend/services/contabilidad.py` (patrón idéntico a `revertir_asiento_comprobante`): busca asientos automáticos del pago, invierte debe↔haber, inserta asiento de tipo `ajuste` con concepto "REVERSIÓN PAGO — ...". Crítico: se llama ANTES del `DELETE FROM pagos` en `eliminar_pago` (la FK aún debe existir para el lookup). Import `revertir_asiento_pago` agregado en `pagos.py` + `import logging as _logging`. El try/except es no-bloqueante (warning en log, no 500). **(2) SQL Views doble-conteo:** vistas `v_saldo_clientes` y `v_saldo_proveedores` en `db/esquema_bd.sql` reemplazadas con subquery pre-agregada de pagos — elimina la multiplicación 1:N de `monto_total` cuando un comprobante tiene múltiples pagos parciales. Migración `db/migrations/2026-04-24_fix_vistas_saldo.sql` NUEVA con `DROP VIEW IF EXISTS` + `CREATE VIEW` para aplicar en producción. **(3) Anti-float inventario:** `backend/routers/inventario.py` línea 68 cambiada de `cantidad_nueva: float` a `cantidad_nueva: Decimal`; import `from decimal import Decimal` agregado al tope del archivo. Cierra la única violación de la regla "nunca float" que quedaba en el código. **(4) Auditoría documentada:** `docs/AUDITORIA_SISTEMA_CONTABLE.md` actualizado — todos los hallazgos de Fases H, I, J marcados `[RESUELTO ✅]`, sección bugs extendida con Bug #3 (pago sin reverso contable), bugs #1 y #2 marcados resueltos con detalles del fix aplicado. | `backend/services/contabilidad.py`, `backend/routers/pagos.py`, `backend/routers/inventario.py`, `db/esquema_bd.sql`, `db/migrations/2026-04-24_fix_vistas_saldo.sql` (NUEVO), `docs/AUDITORIA_SISTEMA_CONTABLE.md` | **Listo para commit y push. Todas las correcciones de integridad aplicadas. Próxima fase disponible: Fase 5 (dashboard remoto viewer) o Polish (paginación real, tests E2E Playwright).** |
| 2026-04-25 | Claude | **BUILD COMPLETO — Instalador ERP Universal Setup 4.0.0.exe reconstruido con todos los fixes del día.** PM solicitó rebuild del instalador. Se ejecutaron 3 pasos: (1) `npm run build` Next.js → 33 rutas incluyendo `/dashboard` con selector de período; (2) `pyinstaller backend.spec --noconfirm` → nuevo `dist/backend/backend.exe` (29MB, timestamp 23:43) con todos los fixes: `dashboard.py` (período + NOT IN anulado/rechazado), `comprobantes.py` (usuario_carga_id), `cierre_contable.py` (try/except), `inventario.py` (Decimal), `pagos.py` + `contabilidad.py` (revertir_asiento_pago); (3) `npm run electron:build` → `frontend/dist/ERP Universal Setup 4.0.0.exe` (258MB, timestamp 00:01) con `win-unpacked/resources/backend/backend.exe` de 23:43 confirmado. El instalador embebe el backend actualizado. Condiciones: requiere DATABASE_URL Supabase en primer arranque, SmartScreen advierte por falta de firma de código. | `frontend/dist/ERP Universal Setup 4.0.0.exe` (258MB), `dist/backend/backend.exe` (29MB) | **Instalador listo para distribuir. Copiar `frontend/dist/ERP Universal Setup 4.0.0.exe` a cualquier PC Windows. Primer arranque: pide DATABASE_URL, GEMINI_KEY, email/contraseña admin, nombre empresa. SmartScreen: clic en "Más información" → "Ejecutar de todas formas".** |
| 2026-04-25 | Claude | **DASHBOARD — Selector de período global en todos los componentes.** PM solicitó agregar filtro de tiempo a cada gráfico/cuadro del dashboard. Solución: selector de período unificado al tope que afecta todos los componentes simultáneamente. **Backend:** todos los endpoints de `/dashboard/*` reciben ahora `?desde=YYYY-MM-DD&hasta=YYYY-MM-DD` opcionales (`resumen`, `flujo-mensual`, `top-clientes`, `medios-pago`, `ultimos-comprobantes`). El `flujo-mensual` con rango explícito usa CTE recursiva para generar la serie de meses; sin rango mantiene el comportamiento anterior (`meses=N`). **Frontend:** estado `periodoId` con 7 opciones (Este mes / Mes anterior / 3 meses / 6 meses / Este año / Último año / Todo) que se muestra como grupo de botones en el encabezado. Cada opción calcula `desde`/`hasta` al inicializar el componente. Todas las React Query keys incluyen `periodoId` como parte de la clave → recalculan automáticamente al cambiar período. El panel lateral "Flujo del mes actual" se renombra a "Resumen del período" y muestra los totales acumulados del rango completo (no solo el último mes). Cada sección muestra la etiqueta del período seleccionado. `api.ts` actualizado con firma extendida para los 5 métodos del dashboardApi. Build exitoso, TypeScript sin errores. | `backend/routers/dashboard.py`, `frontend/src/app/(app)/dashboard/page.tsx`, `frontend/src/lib/api.ts` | **Listo. El usuario debe refrescar el browser (F5) para ver el nuevo build. El frontend se reconstruyó y se reinició. Próxima fase disponible: Fase 5 (dashboard remoto viewer), Polish, o cualquier otra mejora.** |
| 2026-04-25 | Claude | **FIX DASHBOARD — Lógica de ingresos/egresos corregida + fix crítico lista comprobantes.** PM reportó dos problemas: (1) lista de comprobantes mostraba "Sin comprobantes" — causa: `LEFT JOIN usuarios u ON u.id = c.usuario_id` usaba columna incorrecta (`usuario_id` no existe; la real es `usuario_carga_id`), generando error SQL silencioso → empty result. Fix: cambiar a `c.usuario_carga_id` en `backend/routers/comprobantes.py`. (2) Dashboard mostraba ingresos=0 — causa: los tres endpoints `flujo-mensual`, `top-clientes` y `resumen` filtraban únicamente `estado_validacion = 'confirmado'`, excluyendo comprobantes en `pendiente_revision` (estado por defecto del OCR). Aclaración de negocio: `pendiente_revision` es validación de datos OCR, NO indica falta de pago. Una venta contado cargada por OCR es ingreso inmediato aunque esté en revisión. Fix: cambiar filtro a `NOT IN ('anulado', 'rechazado')` en los tres endpoints de `backend/routers/dashboard.py`. También se saneó la migración `periodos_contables` — `verificar_periodo_abierto()` envuelta en try/except para tolerar entorno dev sin Fase J aplicada. Verificación: `flujo-mensual` muestra ₲3.570.000 ingresos en Diciembre; `top-clientes` muestra 2 clientes con totales correctos; `comprobantes/` devuelve 4 rows. | `backend/routers/comprobantes.py` (fix usuario_carga_id), `backend/routers/dashboard.py` (fix NOT IN anulado/rechazado), `backend/routers/cierre_contable.py` (try/except en verificar_periodo_abierto) | **Sistema estable. Próxima fase disponible: Fase 5 (dashboard remoto viewer), Fase H-visor adjuntos, o Polish paginación.** |
| 2026-04-25 | Claude | **BUG FIX CRÍTICO — flujo-mensual no devolvía datos con rango explícito.** PM reportó que el dashboard mostraba ingresos/egresos SOLO con período "Todo" y nada con "Últimos 6 meses", "Este año", etc. Causa raíz: la rama `if desde is not None` de `/dashboard/flujo-mensual` usaba `WITH RECURSIVE` con parámetros nombrados del tipo `:desde::date` — asyncpg confunde el doble operador `::` (cast PostgreSQL) con el delimitador de parámetro `:`, rompiendo el binding de variables y haciendo que el CTE no genere datos. La rama `else` ("Todo") usa `generate_series` + f-strings (sin parámetros en la CTE) y funcionaba. Fix: eliminar el `WITH RECURSIVE` y reemplazarlo por el mismo patrón `generate_series` que ya funcionaba, calculando el `inicio` del mes como f-string seguro (Python `date.isoformat()` → siempre YYYY-MM-DD). También se agregó `::date` explícito en el JOIN (`date_trunc('month', c.fecha_emision)::date = s.mes`) para consistencia de tipos en ambas ramas. Backend reiniciado con uvicorn. | `backend/routers/dashboard.py` | **Fix committed y pushed (commit 23d8288). Backend corriendo en localhost:8000. Verificar en browser: seleccionar "Últimos 6 meses" o "Este año" y confirmar que el gráfico muestra barras para Dic 2025 / Ene 2026.** |
| 2026-04-24 | Gemini | **AUDITORÍA COMPLETA DEL SISTEMA CONTABLE.** Revisión exhaustiva de toda la funcionalidad contable del ERP. **Hallazgos críticos (🔴):** (1) `plan_cuentas` es tabla muerta — existe en SQL con 17 cuentas seed pero tiene CERO uso en backend/frontend (grep confirma 0 referencias). (2) No existe libro diario ni asientos contables (partida doble inexistente). (3) No hay estados financieros (balance general, estado de resultados, balance de comprobación). (4) Tablas `cuentas_banco` y `movimientos_banco` existen en SQL pero no tienen router, CRUD ni UI — solo backup/wipe las toca. **Hallazgos altos (🟠):** (5) Sin reportes fiscales IVA (Libro Compras/Ventas/Liquidación). (6) Sin aging de saldos (antigüedad de cuentas por cobrar/pagar). (7) Notas de Crédito/Débito sin flujo contable real (no vinculan a factura origen). (8) Sin retenciones. **Hallazgos medios (🟡):** Presupuestos, centros de costo, multi-moneda real, cierre contable. **Bugs técnicos:** `inventario.py:68` usa `float` (viola regla nunca-float), vista `v_saldo_clientes` con posible doble-conteo. **Lo que funciona bien (✅):** CRUD comprobantes, pagos, cuentas corrientes, dashboard (7 endpoints), OCR, chatbot (19 tools), exportación Excel (4 reportes), auditoría JSONB, condición contado/crédito, adjuntos, multi-tenant. **Propuesta:** 3 fases nuevas (H=contabilidad real, I=bancos+fiscal, J=avanzado) con schemas SQL, endpoints y páginas frontend detallados. Documento completo en `docs/AUDITORIA_SISTEMA_CONTABLE.md`. CLAUDE.md actualizado con Fases H/I/J y referencia al documento de auditoría. | `docs/AUDITORIA_SISTEMA_CONTABLE.md` (NUEVO), `CLAUDE.md`, `BITACORA_COLABORATIVA.md` | **Pendiente aprobación PM (gfcar) → Claude ejecuta Fase H. Claude: leer `docs/AUDITORIA_SISTEMA_CONTABLE.md` ANTES de implementar.** |
| 2026-04-29 | Claude | **INTEGRACIÓN DATOS EMPRESA + GEMINI API + REBUILD INSTALADOR.** PM entregó 3 archivos Excel con datos reales de la empresa. **(1) Importación de datos históricos:** creado `tools/importar_datos_empresa.py` que lee los Excel y popula la DB SQLite con: 21 clientes (CEISA, CADENA REAL, CAFSA, FARMA, COSCOM, etc.), 19 proveedores (ALBIS, RODAO PLAST, TRAFALGAR, etc.), 3 categorías de inventario (Materias Primas / Insumos y Envases / Productos Terminados), 115 items de inventario (69 MP + 34 insumos + 12 prod terminados con costos reales), 133 comprobantes de venta (facturas 2023–2026 con montos y saldos pendientes). **(2) Seed para nuevas instalaciones:** generado `backend/core/seed_empresa.py` con todos los datos embebidos; `migrations.py` modificado para llamar `_seed_datos_empresa_sqlite()` tras bootstrap — las nuevas instalaciones arrancan con datos precargados. **(3) Gemini API Key:** `backend/.env` actualizado con `GEMINI_API_KEY=<REDACTADO-2026-05-17 — clave expuesta histórica, rotar urgente; ver docs/ROTACION_SECRETS.md>`; `frontend/electron/config.html` actualizado para pre-rellenar el campo en el wizard de configuración. **(4) Rebuild completo:** PyInstaller backend.exe (29.8MB, 29/4 10:53) + Next.js standalone + electron-builder NSIS → `ERP Universal Setup 4.0.0.exe` (270.4MB, 29/4 11:29) listo en `frontend/dist/`. | `tools/importar_datos_empresa.py` (NUEVO), `backend/core/seed_empresa.py` (NUEVO), `backend/core/migrations.py`, `backend/.env`, `frontend/electron/config.html`, `dist/backend/backend.exe` (29.8MB), `frontend/dist/ERP Universal Setup 4.0.0.exe` (270.4MB) | **Instalador actualizado listo. Al instalar en nueva PC: wizard ya tiene la Gemini key pre-cargada. Los datos de clientes, proveedores e inventario se cargan automáticamente en el primer arranque. Para importar datos en la instalación dev actual ya están en `%APPDATA%/ERP Universal/erp.db`.** |
| 2026-04-28 | Claude | **BUG CRÍTICO RESUELTO — Login fallaba en instalador por CORS + ruta de recursos.** Diagnóstico completo del fallo de login en el .exe instalado: (1) Bug principal: el frontend cargaba en `http://127.0.0.1:3000` (lo abre Electron) pero enviaba peticiones a `http://localhost:8000`; el CORS del backend solo permitía `localhost:3000` no `127.0.0.1:3000` — navegador bloqueaba silenciosamente. (2) Bug secundario: `paths.py` fallaba con `PermissionError` al intentar crear directorio dentro de `C:\Program Files` cuando `ERP_RESOURCES_DIR` no estaba seteado (ejecución manual sin Electron). **Fixes aplicados:** `backend/main.py` — agregadas origins `http://127.0.0.1:3000/3001/8000` al middleware CORS. `backend/core/paths.py` — fallback inteligente: intenta directorio local, si es read-only usa `%APPDATA%/ERP Universal/resources`. `tools/build_exe.py` — hornea `NEXT_PUBLIC_API_URL=http://127.0.0.1:8000` en el bundle Next.js durante build (antes era `undefined` → fallaba con `localhost` en IPv6-only). **Rebuild completo ejecutado:** PyInstaller (backend.exe) + Next.js build con NEXT_PUBLIC_API_URL correcto + electron-builder NSIS. Nuevo instalador generado: `258MB, 28/4 23:08`. Copiado al Escritorio. | `backend/main.py`, `backend/core/paths.py`, `tools/build_exe.py`, `frontend/dist/ERP Universal Setup 4.0.0.exe` (258MB, 23:08), `Desktop/ERP Universal Setup 4.0.0.exe` | **Instalar el nuevo .exe del Escritorio (23:08). Desinstalar el anterior desde Configuración > Aplicaciones primero. En el wizard: poner nombre de empresa, usuario y contraseña. En el login: usar exactamente esas mismas credenciales.** |
| 2026-04-27 | Claude | **REBUILD INSTALADOR + DOCUMENTACIÓN PARA DISTRIBUCIÓN.** PM solicitó garantizar que el `.exe` instalable funcione idéntico en otra PC y crear instructivos. **Build actualizado:** ejecutado pipeline completo `npm run build` (Next.js standalone, 33 rutas) + `pyinstaller backend.spec --noconfirm` (`backend.exe` 28.4MB, timestamp 20:53) + `npm run electron:build` (Setup.exe 257.9MB, timestamp 21:02). Verificado: `backend.exe` dentro de `win-unpacked/resources/backend/` coincide en timestamp con el output de PyInstaller (fix flujo-mensual `generate_series` incluido); 10 migraciones SQL en `_internal/db/migrations/`; `frontend/standalone/server.js` presente. **Auditoría del flujo de instalación confirmada en `electron/main.js`:** el primer arranque sin `%APPDATA%/ERP Universal/backend.env` lanza ventana de configuración pidiendo DATABASE_URL, SUPABASE_URL/KEY, GEMINI_API_KEY, admin email/password/nombre y empresa nombre — los datos se persisten localmente y el backend.exe se levanta con `ERP_ENV_FILE` apuntando ahí. **Documentos generados** (estilo manual moderno con paleta slate/verde, callouts TIP/OJO/NOTA): `docs/Manual_ERP_Universal.pdf` (6 páginas, uso diario por módulo) y `docs/Instalacion_ERP_Universal.pdf` (4 páginas, paso a paso desde descargar `.exe` hasta primer login + troubleshooting). También `docs/Guia_ERP_Universal.pdf` (versión corporativa anterior). | `frontend/dist/ERP Universal Setup 4.0.0.exe` (257.9MB, 27/4 21:02), `dist/backend/backend.exe` (28.4MB), `tools/generar_manual_pdf.py`, `tools/generar_instalacion_pdf.py`, `tools/generar_guia_pdf.py`, `docs/Manual_ERP_Universal.pdf`, `docs/Instalacion_ERP_Universal.pdf`, `docs/Guia_ERP_Universal.pdf` | **Instalador listo para distribuir. Una sola PC con: el `.exe` (258MB) + URL Supabase + clave Gemini + credenciales admin = sistema funcionando en 5min. Próxima fase disponible: 5 (dashboard remoto viewer) o 6 (Timbrado DNIT).** |

| 2026-04-29 | Claude | **Rebuild instalador ERP 4.0.0** — build completo (PyInstaller + Next.js standalone + electron-builder). Instalador actualizado en escritorio con todos los cambios pendientes. | tools/build_exe.py, dist/backend/backend.exe, frontend/dist/ERP Universal Setup 4.0.0.exe | Listo para distribución |
| 2026-04-29 | Claude | **FIX CRÍTICO — "Todo" mostraba menos que "Último año" en el dashboard.** PM reportó que el filtro "Todo" del dashboard mostraba menos datos que "Último año". Causa: dos bugs en `backend/routers/dashboard.py`. **(1) `flujo-mensual` sin desde/hasta:** la rama `else` usaba `meses=6` (parámetro por defecto) con cap de 24 meses → "Todo" solo graficaba 6 meses, mientras "Último año" pasaba `desde/hasta` explícitos y graficaba 12 meses. Fix: cuando `desde=None` se consulta `MIN(fecha_emision)` de comprobantes de la empresa y se genera la serie desde ese mes hasta hoy (cap 120 meses). **(2) `medios-pago` sin desde/hasta:** aplicaba un filtro oculto de "últimos 90 días" cuando no se pasaban fechas. Fix: sin filtro de fecha → todo el historial de pagos. También se limpió el import `timedelta` que quedó sin uso. Rebuild completo (PyInstaller 29.8MB 20:51 + Next.js standalone + electron-builder NSIS) → `ERP Universal Setup 4.0.0.exe` (270.4MB, 29/4 21:01) copiado al Escritorio. | `backend/routers/dashboard.py`, `frontend/dist/ERP Universal Setup 4.0.0.exe` (270.4MB, 21:01), `Desktop/ERP Universal Setup 4.0.0.exe` | **Desinstalar ERP anterior → instalar nuevo .exe del Escritorio (21:01). El filtro "Todo" ahora muestra TODO el historial correctamente.** |
| 2026-04-30 | Claude | **PDF del Plan de Migración v5 + registro como tarea pendiente.** PM solicitó: (1) PDF en el Escritorio explicando el plan en lenguaje accesible para él como PM no técnico; (2) registrar el plan como tarea futura para ejecutar después. **Generado:** `tools/generar_plan_migracion_pdf.py` con ReportLab usando la paleta slate/verde estándar del proyecto (igual estilo que `generar_manual_pdf.py`). PDF de ~11 KB con 8 secciones: portada con resumen en una frase, qué cambia y qué se mantiene (tabla comparativa antes/después), estrategia paralela en otra carpeta, 12 pasos de migración con burbujas numeradas, costos estimados ($0 free tier o $5-7/mes always-on), riesgos con mitigaciones, esfuerzo (~25-35h dev), Definition of Done con 10 checks, y decisión pendiente. Salida: `C:/Users/gfcar/Desktop/Plan_Migracion_v5.pdf`. **Registro como pendiente:** `docs/roadmap/PLAN_MIGRACION_V5_PENDIENTE.md` NUEVO con disparadores (cuándo arrancar), pre-condiciones (cuentas Supabase/Vercel/Railway, JWT nueva, backup pg_dump), próximo paso al arrancar (paso 1 del orden), notas importantes (no tocar Empresa 1, sin Git, JWT custom permanece, Dexie no se toca, OCR no necesita Storage) e histórico de aprobación con 6 hitos del 2026-04-30 (revisión inicial → mejora → Ultraplan → aprobación PM → implementación → ejecución pendiente). | `tools/generar_plan_migracion_pdf.py` (NUEVO), `docs/roadmap/PLAN_MIGRACION_V5_PENDIENTE.md` (NUEVO), `C:/Users/gfcar/Desktop/Plan_Migracion_v5.pdf` (NUEVO, fuera del repo) | **PDF entregado en el Escritorio del PM. Plan registrado en el roadmap como tarea pendiente. Cuando PM diga "arrancar", seguir el Orden de Implementación de PLAN_MIGRACION_V5.md desde el paso 1.** |
| 2026-04-30 | Claude | **PLAN_MIGRACION_V5.md mejorado con secciones A–J (Ultraplan aprobado).** Edición incremental del plan de migración a cloud (Supabase + Vercel + Railway/Render). Hallazgos clave validados contra el código: OCR es stateless (no requiere Storage, va en base64 directo a Gemini en `services/ocr.py:778-819`); JWT custom permanece (`core/security.py`); Dexie es browser-side y migra sin cambios; backend FastAPI long-running NO va a Vercel Functions (debe ir a Railway/Render). Storage solo se necesita para `adjuntos/` y `logos/`. Documentado: puerto 5432 (Session) recomendado para asyncpg, receta puerto 6543 (`statement_cache_size=0` + `NullPool`) como apéndice. Agregadas secciones nuevas: Decisiones de Arquitectura, Migración de Datos Productivos (con `pg_dump`), Variables de Entorno Completas, Registro de Riesgos (7 ítems), Archivos a Eliminar (build desktop), Estimación (~25-35h, $0-7/mes), Definition of Done (10 criterios). Reescrito Orden de Implementación (12 pasos sin referencias a Git/branches por regla del usuario). Tabla "Archivos Críticos" corregida: añadidos `routers/adjuntos.py` (rewrite alto), `main.py` (quitar StaticFiles mount), `services/chatbot.py` (sin cambios). Verificación grep `git\|github\|branch\|rama\|commit` → 0 matches. | `PLAN_MIGRACION_V5.md` | **Plan listo para ejecución. Próximo paso: cuando PM apruebe ejecutar, copiar carpeta `Empresa 1` → `ERP_v5_Cloud`, crear proyecto Supabase y arrancar por el paso 1 del Orden de Implementación. Archivo Empresa 1 NO se toca.** |
| 2026-04-29 | Claude | **FIX DASHBOARD FILTRO PERÍODO + FIX IMPORTACIÓN EXCEL.** PM reportó dos bugs: (1) el filtro de períodos del dashboard no filtraba correctamente; (2) la importación Excel en la página OCR no funcionaba. **Bug 1 — Dashboard timezone:** `toISO()` usaba `.toISOString()` que convierte a UTC; Paraguay en UTC-4 hacía que `new Date(2026,3,1)` → `"2026-03-31"` (un día de desfase), causando que todos los filtros (Este mes, Mes anterior, etc.) consultaran el rango incorrecto. Fix: `toISO()` reescrita para usar `.getFullYear()/.getMonth()/.getDate()` (hora local). Adicionalmente, las constantes de fecha y el array `OPCIONES_PERIODO` se movieron dentro del componente con `useMemo` para recalcular al montar (en lugar de quedar fijas al cargar el módulo). **Bug 2 — Proxy routes localhost vs 127.0.0.1:** los tres archivos de rutas proxy (`/api/ocr/route.ts`, `/api/ocr/confirmar/route.ts`, `/api/ocr/importar-excel/route.ts`) apuntaban a `http://localhost:8000`. En Windows, `localhost` puede resolver a IPv6 `::1` mientras el backend escucha en `127.0.0.1` (IPv4), rompiendo la conexión silenciosamente. Fix: todos los proxies cambiados a `process.env.NEXT_PUBLIC_API_URL ?? 'http://127.0.0.1:8000'`. **Rebuild:** Next.js standalone + PyInstaller + electron-builder NSIS → `ERP Universal Setup 4.0.0.exe` (270.4MB, 29/4 20:27) generado y copiado al Escritorio. | `frontend/src/app/(app)/dashboard/page.tsx`, `frontend/src/app/api/ocr/route.ts`, `frontend/src/app/api/ocr/confirmar/route.ts`, `frontend/src/app/api/ocr/importar-excel/route.ts`, `frontend/dist/ERP Universal Setup 4.0.0.exe` (270.4MB), `Desktop/ERP Universal Setup 4.0.0.exe` | **Instalar nuevo .exe del Escritorio (20:27). Desinstalar el anterior primero desde Configuración > Aplicaciones. Al reinstalar: el filtro de períodos del dashboard ya filtra correctamente y la importación Excel funciona.** |
| 2026-05-01 | Claude | **Sincronización completa con GitHub** (`gfcarlos04-del/Proyecto_empresa1`, rama `feature/fase-a-f-erp-completo`). Empaquetado de todos los cambios pendientes acumulados (22 archivos modificados + 5 nuevos): fixes dashboard/timezone/CTE, proxies OCR `127.0.0.1`, integración datos empresa + Gemini API, splash de Electron, plan de migración v5 (`PLAN_MIGRACION_V5.md`, roadmap pendiente, generador PDF), `db/esquema_sqlite.sql`. Excluidos del commit los logs de build `pyinstaller_out*.txt` (artefactos locales). | 22 archivos modificados (backend routers, electron, frontend OCR/dashboard/login, build_exe), 5 nuevos (`PLAN_MIGRACION_V5.md`, `db/esquema_sqlite.sql`, `docs/roadmap/PLAN_MIGRACION_V5_PENDIENTE.md`, `frontend/electron/splash.html`, `tools/generar_plan_migracion_pdf.py`) | **Repo remoto al día. Próxima IA: trabajo continúa sobre `feature/fase-a-f-erp-completo`; pendiente decisión PM para arrancar Plan Migración v5 (cloud) o Fase 5/6.** |
