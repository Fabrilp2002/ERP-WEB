# ERP_Web v5.0 — backend cloud (FastAPI + Supabase Postgres + Storage)

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.29
httpx==0.28.1

# DB (Postgres async — sin aiosqlite, esto es cloud)
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0

# Validation / settings (pydantic >=2.11.7 requerido por supabase->realtime)
pydantic==2.11.10
pydantic-settings==2.7.0
pydantic[email]==2.11.10
email-validator==2.3.0

# Auth
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# Excel export
openpyxl==3.1.5

# OCR (Gemini Vision + PDF parsing)
pymupdf==1.27.2.3
opencv-python-headless==4.13.0.92
numpy==2.4.6

# Cloud-native (NUEVO en v5)
supabase==2.30.0     # Storage SDK (adjuntos + logos)
resend==2.30.1        # email transaccional (reset password, invitaciones)
